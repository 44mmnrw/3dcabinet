# Проверка загрузки GLTF-модели TSH 700×500×240

## Статус

✅ **Сервер запущен:** http://127.0.0.1:8000  
✅ **Модель на месте:** `public/assets/models/thermocabinets/tsh_700_500_240/`
- `tsh_700_500_240.gltf` (JSON-описание)
- `tsh_700_500_240.bin` (бинарные данные геометрии)

## Структура модели

Из анализа GLTF-файла:

### Узлы (Nodes)
```
MKP92-N-705024 (корневой)
├── BODY (корпус)
├── DOOR (дверца) ← анимируемая
├── INSULATION (изоляция)
├── INSULATION_FRAME (рамка изоляции)
└── PANEL.003 (панель)
    └── DIN_RAIL_40 (группа реек)
        ├── DIN_RAIL_40.001 ← рейка 1
        ├── DIN_RAIL_40.002 ← рейка 2
        └── DIN_RAIL_40.003 ← рейка 3
```

### Mesh-объекты
- 8 mesh-объектов
- 2 материала:
  - `mat_0.001` — основной цвет (RGB: 0.59, 0.64, 0.85)
  - `mat_0.002` — тёмный цвет (RGB: 0.17, 0.19, 0.22)

### Размеры (из bounding box)
- **Ширина:** ~700 мм (X: -0.15 до 0.55)
- **Высота:** ~250 мм (Y: -0.07 до 0.18)
- **Глубина:** ~500 мм (Z: -0.4 до 0.1)

**Важно:** Модель применяет поворот на -90° вокруг Y-оси через quaternion `[0, -0.7071, 0, 0.7071]`

## Что исправлено

### 1. Улучшенная загрузка модели

**Файл:** `public/js/modules/CabinetModel.js`

- ✅ Подробное логирование процесса загрузки
- ✅ Вывод количества узлов и mesh-объектов
- ✅ Обработка ошибок с детализацией
- ✅ Поддержка моделей без `progress.total` (для локальных файлов)

### 2. Поиск дверцы

- ✅ Поиск узла `DOOR` (регистронезависимый)
- ✅ Сохранение исходного поворота дверцы (`doorInitialRotation`)
- ✅ Вывод типа найденного объекта (Mesh, Group, Object3D)
- ✅ Список всех доступных узлов при отсутствии дверцы

### 3. Поиск DIN-реек

- ✅ Поиск всех узлов с `DIN_RAIL` в имени
- ✅ Найдено должно быть 3 рейки: `DIN_RAIL_40.001`, `.002`, `.003`
- ✅ Логирование количества найденных реек

### 4. Анимация дверцы

- ✅ Учёт исходного поворота модели
- ✅ Анимация от `baseRotationY` до `baseRotationY + π/2`
- ✅ Логирование текущего и целевого угла
- ✅ Подтверждение завершения анимации

### 5. Тестовая загрузка

**Файл:** `public/js/configurator.js`

- ✅ Try-catch блок с детальной обработкой ошибок
- ✅ Вывод пути к модели
- ✅ Подсказки при ошибке загрузки

## Как проверить

### 1. Откройте в браузере

```
http://127.0.0.1:8000/app
```

### 2. Откройте DevTools (F12) → Console

Должны появиться логи:

```
🚀 Инициализация 3D Конфигуратора...
✅ SceneManager инициализирован
✅ InteractionController инициализирован
📦 Загрузка тестового шкафа...
  Путь к модели: /assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.gltf
  Начало загрузки: /assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.gltf
  Загружено байт: 2048508  (или Загрузка: 100%)
  ✓ GLTF загружен успешно
  Узлов в сцене: 1
  Корневые узлы: ["MKP92-N-705024"]
  Mesh-объектов: 8
  ✓ Найдена дверца: "DOOR" (тип: Mesh)
    Исходный поворот дверцы: Euler {_x: 0, _y: -1.5707963267948966, _z: 0}
  ✓ Найдена DIN-рейка: "DIN_RAIL_40.001" (тип: Mesh)
  ✓ Найдена DIN-рейка: "DIN_RAIL_40.002" (тип: Mesh)
  ✓ Найдена DIN-рейка: "DIN_RAIL_40.003" (тип: Mesh)
  Всего DIN-реек: 3
  Размеры модели: 700 × 251 × 500 мм
✅ Модель загружена: TSH 700×500×240 (cabinet_...)
✅ Шкаф добавлен: TSH 700×500×240 в позицию (0, 0, 0)
✅ 3D Конфигуратор готов к работе
```

### 3. Визуальная проверка

В центре 3D-сцены должен появиться **серый шкаф** (цвет `0xd0d0d0`).

### 4. Проверка дверцы

**Способ 1: Double Click**
- Дважды кликните по шкафу → дверца должна открыться с анимацией

**Способ 2: Консоль**
```javascript
const cabinet = configurator.getCabinets()[0];
cabinet.toggleDoor(true);
```

**Ожидаемый лог:**
```
🚪 Дверца открывается
  Текущий Y: -1.57, Целевой Y: 0.00
  ✓ Анимация завершена, финальный Y: 0.00
```

### 5. Проверка DIN-реек

```javascript
const cabinet = configurator.getCabinets()[0];
console.log('DIN-рейки:', cabinet.dinRails);
// Должно вывести массив из 3 объектов
```

## Возможные проблемы

### ❌ Модель не загружается

**Логи:**
```
❌ Ошибка загрузки модели /assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.gltf
  Тип ошибки: Error
  Сообщение: Failed to load ...
```

**Решение:**
1. Проверьте путь к файлам:
   ```
   public/assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.gltf
   public/assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.bin
   ```

2. Проверьте, что оба файла доступны через браузер:
   - http://127.0.0.1:8000/assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.gltf
   - http://127.0.0.1:8000/assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.bin

3. Очистите кеш браузера (Ctrl+Shift+R)

### ❌ Дверца не найдена

**Логи:**
```
⚠ Дверца не найдена в модели TSH 700×500×240
  Доступные узлы: [...]
```

**Решение:**
- Проверьте список узлов в консоли
- Убедитесь, что есть узел с именем `DOOR`
- Если дверца называется иначе, добавьте имя в массив `doorNames` в `CabinetModel.js`

### ❌ DIN-рейки не найдены

**Логи:**
```
⚠ DIN-рейки не найдены в модели TSH 700×500×240
```

**Решение:**
- Аналогично дверце, проверьте список узлов
- Добавьте нужные имена в массив `railNames`

### ❌ Модель перевернута/повёрнута неправильно

**Причина:** GLTF применяет поворот через quaternion

**Решение:**
- Дверца учитывает исходный поворот через `doorInitialRotation`
- Для корпуса: модель загружается "как есть" из GLTF

## Следующие шаги

После успешной загрузки модели:

1. **Тестирование взаимодействия:**
   - Click → выбор
   - Shift + Drag → перемещение
   - R → поворот
   - Delete → удаление

2. **Добавление второго шкафа:**
   ```javascript
   await configurator.addCabinet('floor');
   ```
   
3. **Изменение цвета:**
   ```javascript
   configurator.changeCabinetColor(cabinet.id, '#3498db');
   ```

4. **Добавление оборудования на DIN-рейки** (требует реализации `EquipmentModel`)

---

**Дата:** 7 ноября 2025  
**Проект:** 3Cabinet  
**Версия конфигуратора:** 1.0.0
