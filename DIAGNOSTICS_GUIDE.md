# Диагностика проблем с загрузкой 3D-модели

## Текущий статус

🔧 **Включен режим диагностики**

Файл `resources/views/configurator/index.blade.php` временно переключён на `diagnostics.js` вместо `configurator.js`.

## Как проверить

### 1. Обновите страницу в браузере

```
http://127.0.0.1:8000/app
```

**Нажмите Ctrl+Shift+R** (жёсткая перезагрузка с очисткой кеша)

### 2. Откройте DevTools

- **Windows/Linux:** F12 или Ctrl+Shift+I
- **Mac:** Cmd+Option+I

### 3. Перейдите на вкладку Console

Вы должны увидеть:

```
=== ДИАГНОСТИКА 3D КОНФИГУРАТОРА ===
1. Проверка контейнера...
✅ Контейнер найден
  - clientWidth: 1200 (должно быть > 0)
  - clientHeight: 800 (должно быть > 0)
  ...

2. Проверка Three.js...
✅ Three.js загружен
  - Версия: 167
  ...

3. Проверка GLTFLoader...
✅ GLTFLoader загружен
  ...

4. Проверка OrbitControls...
✅ OrbitControls загружен
  ...

5. Проверка модулей конфигуратора...
✅ Все модули загружены
  ...

6. Проверка путей к модели...
  ✅ GLTF файл доступен (application/json)
  ✅ BIN файл доступен (application/octet-stream)

7. Тестовая инициализация Three.js...
✅ Three.js инициализирован успешно

=== КОНЕЦ ДИАГНОСТИКИ ===
```

## Возможные ошибки и решения

### ❌ Контейнер имеет нулевые размеры

**Логи:**
```
✅ Контейнер найден
  - clientWidth: 0
  - clientHeight: 0
```

**Причина:** CSS не применился или контейнер скрыт

**Решение:**
1. Проверьте, что `configurator.css` загружен (вкладка Network в DevTools)
2. Проверьте CSS-правила для `.panel-center` и `#cabinet-3d-container`
3. Убедитесь, что родительский элемент `.configurator-wrapper` имеет высоту

---

### ❌ Three.js не загружен

**Логи:**
```
❌ Three.js не загружен!
```

**Причина:** Файл `three.module.js` отсутствует или путь неверный

**Решение:**
1. Проверьте наличие файла: `public/js/three.module.js`
2. Откройте в браузере: http://127.0.0.1:8000/js/three.module.js
3. Если 404 — файл отсутствует, нужно его загрузить

---

### ❌ Ошибка импорта модулей

**Логи:**
```
Uncaught SyntaxError: Cannot use import statement outside a module
```

**Причина:** Скрипт не подключён с `type="module"`

**Решение:**
Проверьте в `resources/views/configurator/index.blade.php`:
```php
<script type="module" src="{{ asset('js/diagnostics.js') }}"></script>
```

---

### ❌ GLTF файл недоступен

**Логи:**
```
❌ GLTF файл недоступен: 404 Not Found
```

**Причина:** Файл не на месте или путь неверный

**Решение:**
1. Проверьте структуру:
   ```
   public/
   └── assets/
       └── models/
           └── thermocabinets/
               └── tsh_700_500_240/
                   ├── tsh_700_500_240.gltf
                   └── tsh_700_500_240.bin
   ```

2. Откройте в браузере:
   - http://127.0.0.1:8000/assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.gltf
   - http://127.0.0.1:8000/assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.bin

3. Если 404 — переместите файлы в правильное место

---

### ❌ CORS ошибка

**Логи:**
```
Access to fetch at '...' from origin '...' has been blocked by CORS policy
```

**Причина:** Браузер блокирует загрузку файлов

**Решение:**
- Используйте встроенный сервер Laravel (уже запущен на 127.0.0.1:8000)
- НЕ открывайте файлы напрямую через `file://` протокол

---

### ❌ Ошибка в модуле (SyntaxError)

**Логи:**
```
Uncaught SyntaxError: Unexpected token 'export'
  at SceneManager.js:271
```

**Причина:** Синтаксическая ошибка в коде

**Решение:**
1. Откройте указанный файл и строку
2. Проверьте синтаксис
3. Убедитесь, что все `{}` закрыты

---

## После успешной диагностики

Если все проверки прошли успешно (все ✅), вернитесь к основному скрипту:

### Откройте файл `resources/views/configurator/index.blade.php`

Замените:
```php
@push('scripts')
    {{-- Диагностика (временно) --}}
    <script type="module" src="{{ asset('js/diagnostics.js') }}"></script>
    {{-- Основной скрипт (закомментирован для диагностики) --}}
    {{-- <script type="module" src="{{ asset('js/configurator.js') }}"></script> --}}
@endpush
```

На:
```php
@push('scripts')
    <script type="module" src="{{ asset('js/configurator.js') }}"></script>
@endpush
```

### Обновите страницу

Теперь должен загрузиться полный конфигуратор с моделью.

---

## Быстрая проверка через консоль

После загрузки `configurator.js` выполните в консоли:

```javascript
// Проверить наличие API
console.log('Configurator:', window.configurator);

// Список шкафов
configurator.getCabinets();

// Первый шкаф
const cabinet = configurator.getCabinets()[0];
console.log('Cabinet:', cabinet);

// Модель загружена?
console.log('Model:', cabinet.model);

// Дверца найдена?
console.log('Door:', cabinet.door);

// DIN-рейки найдены?
console.log('DIN Rails:', cabinet.dinRails);
```

---

## Контрольный список

- [ ] Сервер Laravel запущен (http://127.0.0.1:8000)
- [ ] Страница открыта в браузере
- [ ] DevTools открыт (F12)
- [ ] Вкладка Console активна
- [ ] Диагностика показывает все ✅
- [ ] Файлы GLTF и BIN доступны
- [ ] Контейнер имеет ненулевые размеры
- [ ] Three.js загружен (версия 167)
- [ ] Все модули импортированы

---

**Следующий шаг:** Скопируйте **все логи из консоли** и отправьте для анализа, если есть ошибки.
