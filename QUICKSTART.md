# 🚀 Быстрый старт 3Cabinet (Laravel)

## ✅ Проект успешно перенесён!

Все файлы из `3dcabinet` перенесены в Laravel framework (`cabinet-calc`).

## 📋 Что было сделано:

1. ✅ **База данных** - настроена в `.env`
2. ✅ **Контроллеры** - созданы для Landing, Configurator, Admin
3. ✅ **Роуты** - настроены все маршруты
4. ✅ **Views** - конвертированы в Blade шаблоны
5. ✅ **Статические файлы** - CSS, JS, fonts, images скопированы в `public/`
6. ✅ **Миграции БД** - созданы для projects, equipment, cabinet_configurations
7. ✅ **Three.js** - библиотеки и модули перенесены

## 🏃 Запуск проекта (3 шага):

### Шаг 1: Запустите миграции
```powershell
cd c:\laragon\www\cabinet-calc
& "C:\laragon\bin\php\php-8.3.26-Win32-vs16-x64\php.exe" artisan migrate
```

### Шаг 2: Запустите сервер
```powershell
& "C:\laragon\bin\php\php-8.3.26-Win32-vs16-x64\php.exe" artisan serve
```

### Шаг 3: Откройте в браузере

**Через встроенный сервер Laravel:**
- **http://localhost:8000** - Главная страница (Landing)
- **http://localhost:8000/app** - 3D Конфигуратор
- **http://localhost:8000/admin** - Админ панель

**Через Laragon (Apache):**

⚠️ **Требуется настройка виртуального хоста!** (см. VHOST_SETUP.md)

После настройки:
- **http://cabinet-calc.test** - Главная страница (Landing)
- **http://cabinet-calc.test/app** - 3D Конфигуратор
- **http://cabinet-calc.test/admin** - Админ панель

## 📁 Структура проекта:

```
cabinet-calc/
├── app/Http/Controllers/          # Контроллеры
│   ├── LandingController.php
│   ├── ConfiguratorController.php
│   └── AdminController.php
│
├── resources/views/               # Blade шаблоны
│   ├── layouts/app.blade.php      # Основной layout
│   ├── partials/                  # Header, Footer, SVG
│   ├── landing/index.blade.php    # Главная страница
│   ├── configurator/index.blade.php  # 3D конфигуратор
│   └── admin/index.blade.php      # Админка
│
├── public/                        # Публичные файлы
│   ├── css/                       # Все стили
│   ├── js/                        # JavaScript + Three.js
│   ├── fonts/                     # Шрифты
│   ├── images/                    # Изображения
│   └── assets/                    # SVG спрайты
│
├── database/migrations/           # Миграции БД
├── routes/web.php                 # Роуты
└── .env                          # Конфигурация (БД и др.)
```

## 🎯 Основные URL:

| URL | Описание | Контроллер |
|-----|----------|------------|
| `/` | Landing страница | LandingController |
| `/app` | 3D Конфигуратор | ConfiguratorController |
| `/admin` | Админ панель | AdminController |

## 🗄️ База данных:

### Таблицы:
1. **projects** - Проекты пользователей
2. **equipment** - Каталог оборудования
3. **cabinet_configurations** - Конфигурации шкафов

## 🔧 Полезные команды:

```powershell
# Очистка кеша
php artisan cache:clear

# Просмотр всех роутов
php artisan route:list

# Откат миграций
php artisan migrate:rollback

# Пересоздание таблиц
php artisan migrate:fresh
```

## 🧩 Генерация SVG-спрайта

Иконки лежат в `public/assets/icons`. Сгенерированный спрайт — `public/assets/sprite/sprite.svg`.

Команды:

```powershell
# Разовая сборка спрайта
npm run build:icons

# Режим наблюдения (пересборка при изменениях SVG)
npm run icons:watch

# Удобный режим разработки: собрать и включить наблюдение
npm run icons:dev
```

Использование в Blade-шаблонах:

```blade
<svg class="icon"><use xlink:href="{{ asset('assets/sprite/sprite.svg#icon-save') }}"></use></svg>
```

## 📚 Документация:

- **3DCABINET_README.md** - Полная документация проекта
- **LARAGON_SETUP.md** - Настройка Laragon и виртуальных хостов

## ⚠️ Важные замечания:

1. **PHP путь**: В PowerShell используйте полный путь к PHP или создайте алиас (см. LARAGON_SETUP.md)
2. **База данных**: Убедитесь, что БД `3dcabinet` существует
3. **Миграции**: Запустите `php artisan migrate` перед использованием
4. **Three.js**: Модули находятся в `public/js/libs/`

## 🎉 Готово!

Проект готов к работе. Все файлы перенесены, структура настроена, миграции созданы.

Запустите сервер и начните разработку! 🚀
