# ==================================================
# PRODUCTION ENVIRONMENT — 3Cabinet
# ==================================================
# ВАЖНО: Этот файл — ШАБЛОН для production сервера
# Создайте .env на сервере и заполните реальными данными
# ==================================================

# ----- Основные настройки приложения -----
APP_NAME="3Cabinet"
APP_ENV=production                      # ⚠️ ОБЯЗАТЕЛЬНО production
APP_KEY=                                # 🔑 Сгенерировать: php artisan key:generate
APP_DEBUG=false                         # ⚠️ ОБЯЗАТЕЛЬНО false в production
APP_URL=https://ваш-домен.ru            # 🌐 Ваш реальный URL с https://

APP_LOCALE=ru
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=ru_RU

APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

# ----- Логирование -----
LOG_CHANNEL=daily                       # daily = ротация по дням
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error                         # error = только ошибки в production

# ----- База данных -----
# Получите эти данные в панели управления хостингом
DB_CONNECTION=mysql
DB_HOST=localhost                       # Обычно localhost (не 127.0.0.1)
DB_PORT=3306
DB_DATABASE=имя_базы_данных             # 📝 Укажите название БД
DB_USERNAME=пользователь_базы           # 📝 Укажите пользователя
DB_PASSWORD="пароль_базы"               # 📝 Укажите пароль (в кавычках!)

# ----- Сессии -----
SESSION_DRIVER=database                 # database = хранение в БД (для масштабирования)
SESSION_LIFETIME=120                    # 2 часа
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=.ваш-домен.ru            # 📝 Ваш домен (с точкой для поддоменов)
SESSION_SECURE_COOKIE=true              # ⚠️ true для HTTPS (обязательно!)

# ----- Трансляция событий -----
BROADCAST_CONNECTION=log

# ----- Файловая система -----
FILESYSTEM_DISK=local                   # local = storage/app

# ----- Очереди -----
QUEUE_CONNECTION=database               # database = очереди в БД (можно redis/sqs)

# ----- Кэширование -----
CACHE_STORE=database                    # database или redis (если доступен)
# CACHE_PREFIX=3cabinet_                # Опционально: префикс для кеша

# ----- Memcached (если используется) -----
MEMCACHED_HOST=127.0.0.1

# ----- Redis (если используется) -----
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ----- Почта -----
# Настройте согласно вашему почтовому провайдеру
MAIL_MAILER=smtp
MAIL_HOST=smtp.yandex.ru                # 📝 Или smtp.gmail.com, smtp.mail.ru и т.д.
MAIL_PORT=465                           # 465 для SSL, 587 для TLS
MAIL_USERNAME=ваш@email.ru              # 📝 Ваш email
MAIL_PASSWORD="пароль_от_почты"         # 📝 Пароль от почты (в кавычках!)
MAIL_ENCRYPTION=ssl                     # ssl или tls
MAIL_FROM_ADDRESS=noreply@ваш-домен.ru  # 📝 Email отправителя
MAIL_FROM_NAME="${APP_NAME}"

# ----- AWS S3 (если используется) -----
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false

# ----- Pusher (если используется) -----
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_HOST=
# PUSHER_PORT=443
# PUSHER_SCHEME=https
# PUSHER_APP_CLUSTER=mt1

# ----- Vite (не используется в 3Cabinet) -----
VITE_APP_NAME="${APP_NAME}"

# ==================================================
# ИНСТРУКЦИИ ПО ЗАПОЛНЕНИЮ
# ==================================================
#
# 1. Скопируйте этот файл на сервер как .env:
#    cp .env.production.example .env
#
# 2. Сгенерируйте APP_KEY:
#    php artisan key:generate
#
# 3. Заполните данные БД (получите в панели хостинга):
#    - DB_HOST (обычно localhost)
#    - DB_DATABASE (название вашей БД)
#    - DB_USERNAME (пользователь БД)
#    - DB_PASSWORD (пароль БД, в кавычках!)
#
# 4. Укажите ваш домен:
#    - APP_URL=https://ваш-домен.ru
#    - SESSION_DOMAIN=.ваш-домен.ru
#
# 5. Настройте почту (опционально):
#    - MAIL_HOST, MAIL_USERNAME, MAIL_PASSWORD
#
# 6. Проверьте настройки:
#    php artisan config:show
#
# 7. Очистите кеш и создайте новый:
#    php artisan config:cache
#    php artisan route:cache
#    php artisan view:cache
#
# ==================================================
# БЕЗОПАСНОСТЬ
# ==================================================
#
# ⚠️  НЕ коммитьте .env в Git!
# ⚠️  Файл .env должен быть ТОЛЬКО на сервере!
# ⚠️  Права на .env: chmod 600 .env (только владелец может читать)
# ⚠️  APP_DEBUG=false в production (иначе видны все ошибки)
# ⚠️  SESSION_SECURE_COOKIE=true для HTTPS
#
# ==================================================
