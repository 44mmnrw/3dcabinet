# 🔄 Обновление проекта на боевом сервере

## После `git pull` на сервере

### 1. Обновить PHP зависимости
```bash
composer install --no-dev --optimize-autoloader
```

### 2. Обновить Node зависимости и пересобрать фронтенд
```bash
npm ci
npm run build:icons  # Если используете SVG-спрайт
npm run build        # Сборка фронтенда через Vite
```

### 3. Применить миграции (если есть новые)
```bash
php artisan migrate --force
```

### 4. Очистить старый кеш Laravel
```bash
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan cache:clear
```

### 5. Создать новый кеш (оптимизация)
```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

### 6. Проверить права доступа
```bash
chmod -R 775 storage
chmod -R 775 bootstrap/cache
```

---

## ⚡ Быстрая команда (всё сразу)

```bash
# Обновить зависимости
composer install --no-dev --optimize-autoloader && \
npm ci && \
npm run build:icons && \
npm run build && \

# Применить миграции
php artisan migrate --force && \

# Очистить и пересоздать кеш
php artisan optimize:clear && \
php artisan config:cache && \
php artisan route:cache && \
php artisan view:cache && \

# Права
chmod -R 775 storage bootstrap/cache && \

echo "✅ Обновление завершено!"
```

---

## ⚠️ Важные моменты

1. **Проверьте `.env`** — убедитесь что:
   - `APP_ENV=production`
   - `APP_DEBUG=false`
   - `APP_URL` указывает на правильный домен

2. **Если фронтенд не пересобирается** — можно пропустить `npm` команды, если `public/build/` уже собран локально и загружен через git

3. **Если ошибки с правами** — попробуйте:
   ```bash
   chmod -R 777 storage bootstrap/cache
   ```

4. **Проверка после обновления**:
   - Откройте сайт в браузере
   - Проверьте консоль браузера на ошибки
   - Проверьте логи: `tail -f storage/logs/laravel.log`

---

## 🔍 Если что-то пошло не так

### Откатить изменения:
```bash
git reset --hard HEAD~1  # Откатить последний коммит
# Или
git checkout <предыдущий-коммит>
```

### Просмотр логов:
```bash
tail -f storage/logs/laravel.log
```

### Проверка статуса миграций:
```bash
php artisan migrate:status
```

