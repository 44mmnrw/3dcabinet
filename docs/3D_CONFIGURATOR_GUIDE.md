# 3D Конфигуратор — Быстрый старт

## Что реализовано

✅ Модульная архитектура (SceneManager, CabinetModel, CabinetManager, InteractionController)  
✅ Загрузка GLTF-моделей шкафов  
✅ Упрощённая 3D-комната (сетка пола, линии стен)  
✅ Поддержка напольных и подвесных шкафов  
✅ Анимированное открытие/закрытие дверцы  
✅ Изменение цвета и загрузка текстур  
✅ Установка оборудования на DIN-рейки  
✅ Collision detection (предотвращение наложения)  
✅ Snap-to-grid и snap-to-nearby (примагничивание)  
✅ Drag&drop (Shift + MouseDown)  
✅ Горячие клавиши (R — поворот, O — дверца, Delete — удаление)  
✅ UI-панель с параметрами выбранного шкафа  

## Запуск

### 1. Убедитесь, что модель шкафа на месте

```
public/assets/models/thermocabinets/tsh_700_500_240/
├── tsh_700_500_240.gltf
└── tsh_700_500_240.bin
```

### 2. Запустите Laravel-сервер

```powershell
cd c:\laragon\www\3dcabinet
php artisan serve
```

### 3. Откройте в браузере

```
http://localhost:8000/app
```

### 4. Проверьте консоль

Должны появиться сообщения:
```
🚀 Инициализация 3D Конфигуратора...
✅ SceneManager инициализирован
✅ InteractionController инициализирован
📦 Загрузка тестового шкафа...
Загрузка TSH 700x500x240: 100%
✅ Модель загружена: TSH 700x500x240 (cabinet_...)
✅ Шкаф добавлен: TSH 700x500x240 в позицию (0, 0, 0)
✅ 3D Конфигуратор готов к работе
💡 Доступные команды: ...
```

## Управление

### Мышь

- **Левая кнопка + перемещение** — вращение камеры (OrbitControls)
- **Колесо мыши** — приближение/отдаление
- **Правая кнопка + перемещение** — панорамирование
- **Click по шкафу** — выбрать (появится фиолетовая рамка)
- **Double Click по шкафу** — открыть/закрыть дверцу
- **Shift + MouseDown на шкафу** — перетащить

### Клавиатура

- **R** — повернуть выбранный шкаф на 90°
- **O** — открыть/закрыть дверцу выбранного шкафа
- **Delete** или **Backspace** — удалить выбранный шкаф
- **Escape** — снять выбор

### Консоль (window.configurator)

```javascript
// Добавить напольный шкаф
await configurator.addCabinet('floor');

// Добавить подвесной шкаф
await configurator.addCabinet('wall');

// Список всех шкафов
configurator.getCabinets();

// Удалить шкаф по ID
configurator.removeCabinet('cabinet_...');

// Открыть/закрыть дверцу
configurator.toggleDoor('cabinet_...');

// Изменить цвет
configurator.changeCabinetColor('cabinet_...', '#ff5733');
```

## Правая панель (при выборе шкафа)

- **Информация:** ID, тип, размеры, состояние дверцы, количество оборудования
- **Кнопки:**
  - "Открыть дверцу" / "Закрыть дверцу"
  - "Повернуть 90°"
  - "Удалить" (красная кнопка)
- **Color Picker:** изменение цвета в реальном времени

## Требования к GLTF-моделям

### Обязательные объекты

1. **Дверца** — объект с именем `door` / `Door` / `дверь`
   - Pivot на оси петель
   - Вращение вокруг Y-оси

2. **DIN-рейки** (опционально) — объекты с именем `din` / `rail` / `DIN_Rail`
   - Mesh-объекты для монтажа оборудования

### Масштаб

- **Единицы:** миллиметры (1 юнит Three.js = 1 мм)
- Реальные размеры: 700×500×240 мм → 700×500×240 единиц

## Структура файлов

```
public/js/
├── configurator.js                 # Главный модуль
├── modules/
│   ├── SceneManager.js            # Сцена, камера, освещение
│   ├── CabinetModel.js            # Модель шкафа (GLTF, дверца, цвет)
│   ├── CabinetManager.js          # Менеджер коллекции (collision, snap)
│   ├── InteractionController.js   # События мыши, клавиатуры
│   └── README.md                  # Подробная документация
├── three.module.js                # Three.js r167
├── OrbitControls.js
└── GLTFLoader.js
```

## Collision Detection & Snap

### Автопозиционирование

При добавлении шкафа система:
1. Проверяет коллизии с существующими шкафами
2. Ищет свободное место (рядом с последним шкафом или в новом ряду)
3. Применяет snap-to-grid (100×100 мм)
4. Применяет snap-to-nearby (примагничивание к соседним в радиусе 50 мм)

### Перетаскивание

При drag&drop (Shift + MouseDown):
- Постоянная проверка коллизий
- Невозможно переместить шкаф на занятое место
- Автоматическое примагничивание к соседним объектам

## Следующие шаги

### Для добавления оборудования на DIN-рейки

1. Создать класс `EquipmentModel` (аналогично `CabinetModel`)
2. Реализовать drag&drop из левой панели (#equipment-catalog)
3. Использовать `cabinet.addEquipment(equipmentModel, railIndex)`

### Для сохранения конфигурации

1. Создать API-маршрут в `routes/api.php`:
   ```php
   Route::post('/configurations', [ConfiguratorController::class, 'save']);
   ```

2. Сериализовать состояние:
   ```javascript
   const config = {
       cabinets: configurator.getCabinets().map(cab => ({
           id: cab.id,
           type: cab.config.type,
           position: cab.position.toArray(),
           rotation: cab.rotation,
           color: cab.config.color,
           doorOpen: cab.isDoorOpen
       }))
   };
   
   fetch('/api/configurations', {
       method: 'POST',
       headers: { 'Content-Type': 'application/json' },
       body: JSON.stringify(config)
   });
   ```

3. Сохранить в БД (таблица `cabinet_configurations`)

## Отладка

### Включить оси координат

В `SceneManager.js` раскомментировать:
```javascript
const axesHelper = new THREE.AxesHelper(1000);
this.scene.add(axesHelper);
```

### Проверить загрузку модели

```javascript
const cabinet = configurator.getCabinets()[0];
console.log('Model:', cabinet.model);
console.log('GLTF:', cabinet.gltf);
console.log('Door:', cabinet.door);
console.log('DIN Rails:', cabinet.dinRails);
```

### Проверить bounding box

```javascript
const cabinet = configurator.getCabinets()[0];
const bbox = cabinet.getBoundingBox();
console.log('Min:', bbox.min);
console.log('Max:', bbox.max);
```

## Известные ограничения

- Collision detection использует AABB (не учитывает поворот геометрии)
- Drag работает только на плоскости пола (Y=0)
- Максимум 20-30 шкафов на сцене (ограничение производительности)

## Документация

Подробная документация модулей: `public/js/modules/README.md`

---

**Версия:** 1.0.0  
**Дата:** 7 ноября 2025  
**Проект:** 3Cabinet
