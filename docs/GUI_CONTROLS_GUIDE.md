# GUI Controls Guide — Панель управления 3D-конфигуратором

## 📋 Обзор

Реализована интерактивная панель управления (как в [glTF Viewer](https://gltf-viewer.donmccurdy.com/)) с использованием библиотеки **dat.GUI**.

Панель позволяет в реальном времени настраивать:
- Рендеринг (exposure, tone mapping)
- Освещение (интенсивность, цвета светов)
- Отображение (wireframe, grid, фон)

---

## 🎛️ Структура панели

### 📺 **Display (Отображение)**

| Контрол | Описание | Значения |
|---------|----------|----------|
| **Wireframe** | Отображение моделей в виде сетки | `true` / `false` |
| **Grid** | Показать/скрыть сетку пола | `true` / `false` (по умолчанию: `true`) |
| **Auto Rotate** | Автоматическое вращение камеры | `true` / `false` |
| **Background** | Цвет фона сцены | Цветовой пикер (по умолчанию: `#f5f5f5`) |

### 🎨 **Rendering (Рендеринг)**

| Контрол | Описание | Диапазон | По умолчанию |
|---------|----------|----------|--------------|
| **Exposure** | Экспозиция (яркость сцены) | `0.1` - `3.0` | `1.0` |
| **Tone Mapping** | Алгоритм тонирования | `Linear`, `ACES Filmic`, `Reinhard`, `Cineon` | `ACES Filmic` |

**Tone Mapping — что это?**
- **Linear**: Линейное отображение (базовое, может быть тусклым)
- **ACES Filmic**: Кинематографическое тонирование (Hollywood-качество) ✅ **Рекомендуется**
- **Reinhard**: Классический HDR-тонировщик (мягкий, без пересветов)
- **Cineon**: Цифровое кино (контрастный, яркие акценты)

### 💡 **Lighting (Освещение)**

**Интенсивность**:
| Свет | Описание | Диапазон | По умолчанию |
|------|----------|----------|--------------|
| **Key Light** | Основной источник света (сверху-справа) | `0` - `5` | `1.2` |
| **Fill Light** | Заполняющий свет (слева) | `0` - `3` | `0.5` |
| **Rim Light** | Контровый свет (сзади) | `0` - `2` | `0.3` |
| **Ambient** | Глобальное рассеянное освещение | `0` - `2` | `0.4` |

**Цвета**:
| Свет | Цвет по умолчанию | Описание |
|------|-------------------|----------|
| **Key Color** | `#ffffff` (белый) | Цвет основного света |
| **Ambient Color** | `#ffffff` (белый) | Цвет рассеянного света |

---

## 🛠️ Техническая реализация

### Файлы

**Библиотека**:
```
public/js/libs/dat.gui.min.js
```

**Интеграция**:
- `SceneManager.js` — метод `addGUI()` (строка ~415)
- `configurator.js` — вызов `sceneManager.addGUI()` (строка ~92)
- `configurator/index.blade.php` — подключение `<script src="dat.gui.min.js">`
- `public/css/configurator.css` — стили `.gui-container` (строка ~271)

### Код создания GUI (SceneManager.js)

```javascript
addGUI() {
    if (typeof dat === 'undefined') {
        console.warn('⚠️ dat.GUI не загружен');
        return;
    }
    
    this.gui = new dat.GUI({
        autoPlace: false,
        width: 300,
        hideable: true,
    });
    
    // Состояние контролов
    this.guiState = {
        exposure: 1.0,
        toneMapping: 'ACESFilmic',
        wireframe: false,
        grid: true,
        autoRotate: false,
        keyIntensity: 1.2,
        fillIntensity: 0.5,
        rimIntensity: 0.3,
        ambientIntensity: 0.4,
        keyColor: '#ffffff',
        fillColor: '#ffffff',
        rimColor: '#ffffff',
        ambientColor: '#ffffff',
        bgColor: '#f5f5f5',
    };
    
    // Папки
    const displayFolder = this.gui.addFolder('Display');
    const renderFolder = this.gui.addFolder('Rendering');
    const lightFolder = this.gui.addFolder('Lighting');
    
    // Контролы...
    displayFolder.add(this.guiState, 'wireframe').onChange((value) => {
        this.scene.traverse((obj) => {
            if (obj.isMesh && obj.material) {
                obj.material.wireframe = value;
            }
        });
    });
    
    renderFolder.add(this.guiState, 'exposure', 0.1, 3.0, 0.01).onChange((value) => {
        this.renderer.toneMappingExposure = value;
    });
    
    lightFolder.add(this.guiState, 'keyIntensity', 0, 5, 0.1).onChange((value) => {
        if (this.keyLight) this.keyLight.intensity = value;
    });
    
    // Размещение GUI справа-сверху
    const guiContainer = document.createElement('div');
    guiContainer.classList.add('gui-container');
    guiContainer.style.position = 'absolute';
    guiContainer.style.top = '10px';
    guiContainer.style.right = '10px';
    guiContainer.style.zIndex = '1000';
    guiContainer.appendChild(this.gui.domElement);
    this.container.appendChild(guiContainer);
}
```

### Стили (configurator.css)

```css
/* GUI-панель */
.gui-container {
    position: absolute;
    top: 10px;
    right: 10px;
    z-index: 1000;
}

/* Фиолетовая тема (как в проекте) */
.gui-container .dg .folder .title {
    background: #8b5cf6 !important; /* Фиолетовый */
    color: white !important;
    font-weight: 600 !important;
}

.gui-container .dg .c:hover {
    background: #f5f3ff !important; /* Светло-фиолетовый при наведении */
}

.gui-container .dg .slider-fg {
    background: #8b5cf6 !important; /* Фиолетовый слайдер */
}
```

---

## 🎯 Использование

### Открыть панель
Панель появляется **автоматически** в правом верхнем углу 3D-сцены при загрузке страницы `/app`.

### Свернуть/развернуть
Нажмите кнопку `Close Controls` (или `Open Controls`) в правом верхнем углу панели.

### Настройка экспозиции (яркости)
1. Откройте папку **Rendering**
2. Двигайте слайдер **Exposure** (влево — темнее, вправо — ярче)
3. Диапазон: `0.1` (очень темно) → `3.0` (очень ярко)
4. **Рекомендуется**: `0.8` - `1.5`

### Изменение Tone Mapping
1. В папке **Rendering** выберите **Tone Mapping**
2. Выпадающий список:
   - `Linear` — базовый (тусклый)
   - `ACES Filmic` — кинематографический ✅ **Лучший выбор**
   - `Reinhard` — HDR без пересветов
   - `Cineon` — контрастный, яркие акценты

### Настройка освещения
1. Откройте папку **Lighting**
2. Настройте интенсивность:
   - **Key Light** — основной свет (от `0` до `5`)
   - **Fill Light** — заполняющий (от `0` до `3`)
   - **Rim Light** — контровый (от `0` до `2`)
   - **Ambient** — рассеянный (от `0` до `2`)
3. Измените цвета через Color Picker (Key Color, Ambient Color)

### Wireframe Mode (сетка модели)
1. В папке **Display** включите **Wireframe**
2. Все 3D-модели отобразятся в виде сетки (для отладки геометрии)

### Изменение фона
1. В папке **Display** нажмите на **Background**
2. Выберите цвет в Color Picker
3. По умолчанию: светло-серый `#f5f5f5`

---

## 🔧 Рецепты настроек

### 🌅 **Тёплая студия** (для презентаций)
```javascript
Exposure: 1.2
Tone Mapping: ACES Filmic
Key Intensity: 1.5
Ambient Intensity: 0.6
Key Color: #fff4e6 (тёплый белый)
Background: #fef3e2
```

### 🌌 **Холодная студия** (техно-стиль)
```javascript
Exposure: 0.9
Tone Mapping: Cineon
Key Intensity: 1.0
Ambient Intensity: 0.3
Key Color: #e0f2ff (холодный белый)
Background: #1a1a2e
```

### 📸 **Фотореалистичный** (как glTF Viewer)
```javascript
Exposure: 1.0
Tone Mapping: ACES Filmic
Key Intensity: 1.2
Fill Intensity: 0.5
Rim Intensity: 0.3
Ambient Intensity: 0.4
Key Color: #ffffff
Background: #f5f5f5
```

### 🔍 **Отладочный режим**
```javascript
Wireframe: true
Grid: true
Exposure: 1.5
Tone Mapping: Linear
Ambient Intensity: 1.0
```

---

## 📊 Производительность

**Влияние на FPS**:
- GUI-панель: **0 FPS** (только CPU при взаимодействии)
- Exposure изменение: **0 FPS** (шейдерная переменная)
- Tone Mapping переключение: **0 FPS** (шейдерный параметр)
- Wireframe mode: **-5-10 FPS** (больше линий для рендеринга)

**Оптимизации**:
- GUI обновляется только при взаимодействии (не каждый кадр)
- Слайдеры используют `.onChange()` (не `.listen()`)
- Папки можно закрывать для экономии места

---

## 🚨 Устранение проблем

### Панель не появляется
**Причина**: dat.GUI не загружен  
**Решение**:
1. Проверьте консоль браузера: должно быть `✅ GUI-панель создана`
2. Убедитесь, что `dat.gui.min.js` в `public/js/libs/`
3. Проверьте `<script src="{{ asset('js/libs/dat.gui.min.js') }}">` в Blade

### Слайдеры не работают
**Причина**: Ссылки на источники света не сохранены  
**Решение**:
1. В методе `setupLighting()` добавить:
   ```javascript
   this.keyLight = keyLight;
   this.fillLight = fillLight;
   this.rimLight = rimLight;
   this.ambientLight = ambientLight;
   ```

### Цвета не меняются
**Причина**: KeyShot материалы перезаписывают цвета  
**Решение**:
- Цвета **Key Light** и **Ambient Light** работают корректно
- Для изменения цветов материалов используйте настройки текстур

### GUI перекрывает контролы камеры
**Решение**:
```css
.gui-container {
    top: 10px;
    right: 10px; /* Убедитесь, что справа достаточно места */
}
```

---

## 🎓 Дополнительные возможности

### Добавить новый контрол

**Пример: добавить слайдер FOV (угол обзора камеры)**:

```javascript
// В addGUI(), после создания displayFolder:
displayFolder.add(this.camera, 'fov', 30, 120, 1).name('FOV').onChange(() => {
    this.camera.updateProjectionMatrix();
});
```

### Сохранить настройки в LocalStorage

```javascript
// При изменении контрола:
.onChange((value) => {
    this.renderer.toneMappingExposure = value;
    localStorage.setItem('exposure', value);
});

// При загрузке:
const savedExposure = localStorage.getItem('exposure');
if (savedExposure) {
    this.guiState.exposure = parseFloat(savedExposure);
    this.renderer.toneMappingExposure = this.guiState.exposure;
}
```

### Скрыть панель по умолчанию

```javascript
this.gui = new dat.GUI({
    autoPlace: false,
    width: 300,
    hideable: true,
    closed: true  // ← Свёрнута по умолчанию
});
```

---

## 📚 Ссылки

- **dat.GUI документация**: https://github.com/dataarts/dat.gui
- **glTF Viewer исходники**: https://github.com/donmccurdy/three-gltf-viewer/blob/main/src/viewer.js#L519
- **Three.js ToneMapping**: https://threejs.org/docs/#api/en/constants/Renderer

---

## ✅ Итоговая реализация

**Что добавлено**:
1. ✅ Библиотека dat.GUI (80KB) в `public/js/libs/`
2. ✅ Метод `SceneManager.addGUI()` с 3 папками контролов
3. ✅ 15 интерактивных параметров (exposure, tone mapping, освещение, отображение)
4. ✅ Фиолетовая тема GUI (соответствует дизайну проекта)
5. ✅ Адаптивность на мобильных (260px ширина)

**Результат**: Пользователь может настраивать рендеринг в реальном времени, как в профессиональных 3D-вьюверах! 🎉
