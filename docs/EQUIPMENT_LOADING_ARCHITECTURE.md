# 🏗️ Архитектура системы загрузки шкафов и оборудования

> **Дата обновления:** 11 ноября 2025  
> **Версия:** 2.0 (оптимизированная архитектура)

## 📋 Обзор

Новая архитектура разделяет ответственность между модулями:
- **Данные** (каталоги) отделены от **логики** (менеджеры)
- **Централизованная загрузка** 3D моделей через `AssetLoader`
- **Масштабируемость** — легко добавлять новые шкафы/оборудование
- **Drag & Drop** — готовность к интеграции UI

---

## 🎯 Workflow: От каталога до сцены

### 1. Добавление шкафа на сцену

```javascript
// 1. Получить данные из каталога
import { getCabinetById } from './data/cabinets-catalog.js';
const cabinetData = getCabinetById('tsh_700_500_240');

// 2. Создать экземпляр CabinetModel
const cabinet = new CabinetModel(cabinetData.model.path, {
    type: cabinetData.specifications.mounting, // 'wall' или 'floor'
    width: cabinetData.dimensions.width,
    height: cabinetData.dimensions.height,
    depth: cabinetData.dimensions.depth,
    name: cabinetData.name
}, renderer, sceneManager);

// 3. Добавить через CabinetManager
await cabinetManager.addCabinet(cabinet);
// → Шкаф загружен, размещён, добавлен на сцену
```

### 2. Добавление оборудования на шкаф

```javascript
// 1. Пользователь выбирает оборудование в боковой панели
const catalogId = 'circuit_breaker_1p';

// 2. Создать экземпляр EquipmentModel
const equipment = await equipmentManager.createEquipment(catalogId);
// → Модель загружена через AssetLoader

// 3. Drag & Drop на шкаф (или клик кнопки)
await equipmentManager.placeOnCabinet(equipment, cabinet, {
    dinRailIndex: 0 // Первая DIN-рейка
});
// → Оборудование размещено, привязано к шкафу
```

---

## 📁 Структура файлов

```
public/js/
├── data/                          # 📊 ДАННЫЕ (каталоги)
│   ├── cabinets-catalog.js        # Каталог шкафов
│   └── equipment-catalog.js       # Каталог оборудования
│
├── modules/                       # ⚙️ ЛОГИКА (менеджеры и модели)
│   ├── AssetLoader.js             # Загрузчик 3D моделей (singleton)
│   ├── SceneManager.js            # Управление сценой, камерой, освещением
│   ├── CabinetManager.js          # Менеджер шкафов
│   ├── CabinetModel.js            # Экземпляр шкафа
│   ├── EquipmentManager.js        # Менеджер оборудования
│   ├── EquipmentModel.js          # Экземпляр оборудования
│   ├── InteractionController.js   # Drag & Drop, клики, выбор
│   └── UIController.js            # [TODO] Управление боковой панелью
│
└── pages/
    └── configurator.js            # Главный оркестратор
```

---

## 🔄 Поток данных

```
┌─────────────────┐
│  КАТАЛОГ        │  cabinets-catalog.js, equipment-catalog.js
│  (JSON данные)  │  → Метаданные, пути к моделям, характеристики
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  CONFIGURATOR   │  configurator.js
│  (оркестратор)  │  → Координирует менеджеры
└────────┬────────┘
         │
         ├──────────────────┬──────────────────┐
         ▼                  ▼                  ▼
┌────────────────┐  ┌────────────────┐  ┌────────────────┐
│ CabinetManager │  │EquipmentManager│  │ SceneManager   │
│                │  │                │  │                │
│ • Коллекция    │  │ • Коллекция    │  │ • Сцена        │
│ • Размещение   │  │ • DIN-рейки    │  │ • Камера       │
│ • Выбор        │  │ • Drag & Drop  │  │ • Освещение    │
└────────┬───────┘  └────────┬───────┘  └────────────────┘
         │                   │
         ▼                   ▼
┌────────────────┐  ┌────────────────┐
│ CabinetModel   │  │ EquipmentModel │
│                │  │                │
│ • load()       │  │ • load()       │
│ • setPosition()│  │ • attachTo()   │
│ • openDoor()   │  │ • setPosition()│
└────────┬───────┘  └────────┬───────┘
         │                   │
         └───────────┬───────┘
                     ▼
            ┌────────────────┐
            │  AssetLoader   │  (singleton)
            │                │
            │ • load()       │
            │ • cache        │
            │ • DRACO        │
            └────────┬───────┘
                     ▼
            ┌────────────────┐
            │  THREE.Scene   │  WebGL рендеринг
            └────────────────┘
```

---

## 📦 Модули: Описание и API

### 1. `cabinets-catalog.js` — Каталог шкафов

**Назначение:** Хранит метаданные всех доступных шкафов.

**Структура записи:**
```javascript
{
    id: 'tsh_700_500_240',              // Уникальный ID
    name: 'ТШ-7-IP54',                  // Отображаемое имя
    description: 'Термошкаф уличный',   // Описание
    category: 'outdoor',                // Категория
    dimensions: { width, height, depth }, // Размеры (мм)
    specifications: { ... },            // Технические характеристики
    features: { hasDoor, hasDinRails }, // Особенности
    model: { path, compressed },        // Путь к .glb файлу
    thumbnail: '...',                   // Превью для UI
    price: 12500,                       // Цена
    inStock: true                       // Доступность
}
```

**API:**
```javascript
getCabinetById(id)           // Получить данные по ID
getCabinetsByCategory(cat)   // Фильтр по категории
getAvailableCabinets()       // Только доступные
```

---

### 2. `equipment-catalog.js` — Каталог оборудования

**Назначение:** Хранит метаданные всего оборудования.

**Структура записи:**
```javascript
{
    id: 'circuit_breaker_1p',           // Уникальный ID
    name: 'Автоматический выключатель', // Отображаемое имя
    category: 'protection',             // Категория
    subcategory: 'circuit_breakers',    // Подкатегория
    specifications: { ... },            // Технические данные
    dimensions: {
        width: 18,    // мм (1 модуль)
        height: 80,   // мм
        depth: 70,    // мм
        modules: 1    // Количество модулей на DIN-рейке
    },
    mounting: {
        type: 'din_rail',      // 'din_rail', 'wall', 'floor'
        orientation: 'vertical',
        snapToGrid: true       // Примагничивание
    },
    model: { path, compressed, scale }, // 3D модель
    price: 245,                         // Цена
    inStock: true                       // Доступность
}
```

**API:**
```javascript
getEquipmentById(id)         // Получить данные по ID
getEquipmentByCategory(cat)  // Фильтр по категории
getDinRailEquipment()        // Только DIN-rail оборудование
getAvailableEquipment()      // Только доступное
```

---

### 3. `AssetLoader.js` — Загрузчик 3D моделей

**Назначение:** Централизованная загрузка и кэширование GLB моделей.

**Особенности:**
- ✅ Singleton (один экземпляр на приложение)
- ✅ Кэширование загруженных моделей
- ✅ DRACO декомпрессия для сжатых моделей
- ✅ Очередь загрузки (избегает дублирования)
- ✅ Клонирование сцен для независимых экземпляров

**API:**
```javascript
const loader = getAssetLoader(); // Получить singleton

// Загрузить модель (с кэшированием)
const model = await loader.load('/path/to/model.glb', {
    useCache: true,    // Использовать кэш
    clone: true,       // Клонировать сцену
    onProgress: (loaded, total, percent) => { ... }
});

// Предзагрузка массива моделей
await loader.preload([
    '/models/cabinet1.glb',
    '/models/equipment1.glb'
], (loaded, total) => {
    console.log(`${loaded}/${total} загружено`);
});

// Очистить кэш (освободить память)
loader.clearCache();

// Статистика
console.log(loader.getCacheStats());
// → { cached: 5, loading: 1, paths: [...] }
```

---

### 4. `CabinetModel.js` — Экземпляр шкафа

**Назначение:** Представляет отдельный шкаф на сцене.

**Особенности:**
- 🔄 Асинхронная загрузка модели
- 📏 Автоматическое масштабирование до нужных размеров
- 🚪 Анимация открытия дверцы
- 🔧 Обнаружение DIN-реек (`findDinRails()`)
- 📦 Управление оборудованием (`addEquipment()`)

**Workflow:**
```javascript
// 1. Создать экземпляр
const cabinet = new CabinetModel('/path/to/model.glb', {
    type: 'floor',   // 'floor' или 'wall'
    width: 700,      // мм
    height: 500,     // мм
    depth: 240,      // мм
    name: 'TSH-7'
}, renderer, sceneManager);

// 2. Дождаться загрузки
await cabinet.loadPromise;

// 3. Установить позицию
cabinet.setPosition(new THREE.Vector3(0, 0, 0));

// 4. Открыть дверь
cabinet.openDoor();

// 5. Добавить оборудование
cabinet.addEquipment(equipmentModel, 0); // DIN-рейка 0
```

---

### 5. `CabinetManager.js` — Менеджер шкафов

**Назначение:** Управляет всеми шкафами на сцене.

**Ответственность:**
- 📦 Коллекция шкафов (`Map<id, CabinetModel>`)
- 🎯 Размещение без коллизий
- ✅ Выбор шкафа
- 🗑️ Удаление шкафа

**API:**
```javascript
// Добавить шкаф
await cabinetManager.addCabinet(cabinetModel);

// Получить шкаф по ID
const cabinet = cabinetManager.getCabinetById(id);

// Выбрать шкаф
cabinetManager.selectCabinet(id);

// Удалить шкаф
cabinetManager.removeCabinet(id);

// Получить все шкафы
const all = cabinetManager.getAllCabinets();
```

---

### 6. `EquipmentModel.js` — Экземпляр оборудования

**Назначение:** Представляет единицу оборудования.

**Особенности:**
- 🔗 Привязка к шкафу (`attachToCabinet()`)
- 📍 Позиционирование
- 🎨 Выделение (select/deselect)
- 🗑️ Удаление (`dispose()`)

**Workflow:**
```javascript
// 1. Создать из каталога
const catalogData = getEquipmentById('circuit_breaker_1p');
const equipment = new EquipmentModel(catalogData);

// 2. Дождаться загрузки
await equipment.loadPromise;

// 3. Установить позицию
equipment.setPosition(x, y, z);

// 4. Привязать к шкафу
equipment.attachToCabinet(cabinet, 0); // DIN-рейка 0

// 5. Выбрать
equipment.select();

// 6. Удалить
equipment.dispose();
```

---

### 7. `EquipmentManager.js` — Менеджер оборудования

**Назначение:** Управляет всем оборудованием на сцене.

**Ответственность:**
- 📦 Коллекция оборудования (`Map<id, EquipmentModel>`)
- 🔨 Создание из каталога
- 🔧 Размещение на DIN-рейках
- 🎯 Поиск свободных слотов
- ✅ Выбор оборудования
- 🗑️ Удаление

**API:**
```javascript
// Создать оборудование из каталога
const equipment = await equipmentManager.createEquipment('circuit_breaker_1p');

// Добавить на сцену (в режиме drag)
equipmentManager.addToScene(equipment);

// Разместить на шкафу
await equipmentManager.placeOnCabinet(equipment, cabinet, {
    dinRailIndex: 0,  // Номер DIN-рейки
    position: { x: 0, modules: 1 } // Опционально
});

// Удалить оборудование
equipmentManager.removeEquipment(equipment.id);

// Получить оборудование шкафа
const cabEquipment = equipmentManager.getEquipmentByCabinet(cabinetId);

// Статистика
console.log(equipmentManager.getStats());
// → { total: 5, placed: 3, selected: 'equipment_123' }
```

---

## 🧪 Тестирование системы

### Шаг 1: Запустить сервер

```powershell
cd c:\laragon\www\3dcabinet
php artisan serve
# → http://127.0.0.1:8000/app
```

### Шаг 2: Добавить шкаф (автоматически при загрузке)

```javascript
// В configurator.js уже есть:
await this.loadTestCabinet();
// → Шкаф tsh_700_500_240 загружен на сцену
```

### Шаг 3: Добавить оборудование через консоль

Откройте DevTools (F12) → Console и выполните:

```javascript
// 1. Добавить автоматический выключатель
await configurator.addTestCircuitBreaker();
// → Выключатель размещён на первой DIN-рейке первого шкафа

// 2. Проверить статистику
configurator.equipmentManager.getStats();
// → { total: 1, placed: 1, selected: null }

// 3. Получить оборудование шкафа
const cabinet = Array.from(configurator.cabinetManager.cabinets.values())[0];
const equipment = configurator.equipmentManager.getEquipmentByCabinet(cabinet.id);
console.log(equipment);
```

---

## 🎨 UI Integration (будущее развитие)

### Боковая панель с оборудованием

```html
<div class="equipment-panel">
    <div class="equipment-category">
        <h3>🛡️ Защита</h3>
        <div class="equipment-item" data-catalog-id="circuit_breaker_1p">
            <img src="/assets/images/equipment/circuit_breaker_thumb.jpg">
            <span>Автоматический выключатель 1P</span>
            <span class="price">245 руб</span>
        </div>
    </div>
</div>
```

### Drag & Drop (псевдокод)

```javascript
// 1. Начало drag (из боковой панели)
equipmentItem.addEventListener('dragstart', (e) => {
    const catalogId = e.target.dataset.catalogId;
    e.dataTransfer.setData('catalogId', catalogId);
});

// 2. Drop на шкаф
cabinet.addEventListener('drop', async (e) => {
    const catalogId = e.dataTransfer.getData('catalogId');
    
    // Создать оборудование
    const equipment = await configurator.addEquipmentFromCatalog(catalogId);
    
    // Разместить на шкафу
    const cabinetId = e.target.userData.cabinetId;
    await configurator.placeEquipmentOnCabinet(equipment, cabinetId);
});
```

---

## 📝 Добавление нового оборудования

### 1. Подготовить 3D модель

- Формат: `.glb` (DRACO compressed для оптимизации)
- Масштаб: Реальные размеры в мм (1 unit = 1 mm)
- Pivot point: Центр крепления к DIN-рейке
- Материалы: PBR (metalness/roughness)

```
public/assets/models/equipment/
└── power_supply_12v/
    ├── power_supply_12v.glb       ← GLB модель
    └── textures/                   ← Текстуры (если не embedded)
```

### 2. Добавить в каталог

**`equipment-catalog.js`:**
```javascript
{
    id: 'power_supply_12v',
    name: 'Блок питания 12V/5A',
    category: 'power',
    subcategory: 'power_supplies',
    
    dimensions: {
        width: 36,    // 2 модуля (2 × 18 мм)
        height: 90,
        depth: 58,
        modules: 2
    },
    
    mounting: {
        type: 'din_rail',
        orientation: 'vertical',
        snapToGrid: true
    },
    
    model: {
        path: '/assets/models/equipment/power_supply_12v/power_supply_12v.glb',
        compressed: true,
        scale: 1.0
    },
    
    thumbnail: '/assets/images/equipment/power_supply_12v_thumb.jpg',
    price: 1850,
    inStock: true
}
```

### 3. Использовать в коде

```javascript
// Добавить на шкаф
const equipment = await configurator.addEquipmentFromCatalog('power_supply_12v');
await configurator.placeEquipmentOnCabinet(equipment, cabinetId, {
    dinRailIndex: 0
});
```

---

## 🚀 Roadmap

### ✅ Реализовано (v2.0)
- ✅ Каталоги шкафов и оборудования (data/)
- ✅ AssetLoader с кэшированием и DRACO
- ✅ EquipmentModel и EquipmentManager
- ✅ Интеграция с configurator.js
- ✅ Тестовый метод `addTestCircuitBreaker()`

### 🔄 В разработке
- 🔄 UIController — боковая панель с оборудованием
- 🔄 Drag & Drop из панели на шкаф
- 🔄 Визуальное выделение (outline) для оборудования
- 🔄 Поиск свободных слотов на DIN-рейке

### 📅 Будущее
- 📅 Snap-to-grid для оборудования
- 📅 Сохранение конфигурации (JSON)
- 📅 Библиотека шаблонов (preset configurations)
- 📅 Экспорт спецификации (PDF)
- 📅 Множественный выбор (Shift+Click)
- 📅 Копирование оборудования (Ctrl+D)

---

## 🐛 Troubleshooting

### Проблема: Оборудование не загружается

**Причина:** Путь к модели неверный или файл отсутствует.

**Решение:**
```javascript
// Проверить каталог
const equipment = getEquipmentById('circuit_breaker_1p');
console.log(equipment.model.path);
// → /assets/models/equipment/circuit_breaker/circuit_breaker.glb

// Проверить файл
fetch(equipment.model.path).then(r => console.log(r.ok));
// → true (если файл существует)
```

### Проблема: Оборудование размещается не на DIN-рейке

**Причина:** DIN-рейки не найдены в шкафу.

**Решение:**
```javascript
const cabinet = cabinetManager.getCabinetById(cabinetId);
console.log(cabinet.dinRails);
// → [Object3D, Object3D] (должно быть >= 1)

// Если пусто, проверить имена в Blender:
// Должны содержать "dinrail" или "din_rail"
```

### Проблема: Кэш AssetLoader не очищается

**Причина:** Модели остаются в памяти WebGL.

**Решение:**
```javascript
// Очистить кэш вручную
const loader = getAssetLoader();
loader.clearCache();

// Проверить статистику
console.log(loader.getCacheStats());
// → { cached: 0, loading: 0, paths: [] }
```

---

## 📚 Справка

**Документация:**
- [Three.js GLTFLoader](https://threejs.org/docs/#examples/en/loaders/GLTFLoader)
- [DRACO Compression](https://google.github.io/draco/)
- [DIN Rails Standard](https://en.wikipedia.org/wiki/DIN_rail)

**Контакты:**
- Разработка: 44mmnrw
- Репозиторий: github.com/44mmnrw/3dcabinet

---

**Последнее обновление:** 11 ноября 2025  
**Статус:** ✅ Архитектура готова к развитию
