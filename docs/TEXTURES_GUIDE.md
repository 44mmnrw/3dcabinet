# Руководство по работе с текстурами в 3DCabinet

## Структура папок текстур

```
public/assets/textures/
├── metal/
│   ├── brushed/
│   │   ├── brushed_albedo.jpg       # Базовый цвет (обязательная)
│   │   ├── brushed_normal.jpg       # Карта нормалей (опционально)
│   │   ├── brushed_roughness.jpg    # Шероховатость (опционально)
│   │   ├── brushed_ao.jpg           # Ambient Occlusion (опционально)
│   │   └── brushed_metalness.jpg    # Металличность (опционально)
│   └── painted/
│       └── ... (аналогично)
├── wood/
│   └── oak/
│       └── ...
└── plastic/
    └── ...
```

## Соглашения по именованию

### Суффиксы файлов текстур:
- `_albedo.jpg` — базовый цвет (Diffuse/Albedo) — **обязательная**
- `_normal.jpg` — карта нормалей (Normal Map)
- `_roughness.jpg` — карта шероховатости (Roughness)
- `_ao.jpg` — затемнение (Ambient Occlusion)
- `_metalness.jpg` — металличность (Metalness)

### Формат файлов:
- **JPG** для большинства текстур (лучшее сжатие)
- **PNG** если нужна прозрачность (alpha-канал)

### Рекомендуемые разрешения:
- **512×512** — для мелких деталей
- **1024×1024** — для основных поверхностей (оптимально)
- **2048×2048** — для крупных объектов высокого качества
- **4096×4096** — только для героев (сильная нагрузка!)

## Применение текстур к шкафу

### Способ 1: При создании шкафа

```javascript
const cabinet = new CabinetModel(modelPath, {
    type: 'floor',
    width: 700,
    height: 500,
    depth: 240,
    name: 'TSH 700×500×240',
    colorScheme: {
        default: 0x673831,
        body: 0x673831,
        door: 0x673831,
        panel: 0x673831,
        insulation: 0xE8E8E8,
        insulationFrame: 0xC0C0C0,
        dinRail: 0xA8A8A8
    },
    // 🎨 ТЕКСТУРЫ (опционально)
    textureScheme: {
        body: '/assets/textures/metal/brushed',          // Корпус — металл
        door: '/assets/textures/metal/painted',          // Дверь — крашеный металл
        panel: '/assets/textures/plastic/white',         // Панель — белый пластик
        insulation: null,                                 // Изоляция без текстуры (цвет)
        insulationFrame: '/assets/textures/metal/chrome', // Рамка — хром
        dinRail: '/assets/textures/metal/zinc'           // DIN-рейка — цинк
    }
});
```

### Способ 2: Изменение текстуры через API

```javascript
// Изменить текстуру части шкафа
await configurator.changeCabinetTexture(
    cabinetId,
    'body',                                    // Название части
    '/assets/textures/metal/brushed'          // Путь БЕЗ суффикса и расширения
);
```

## Соответствие частей модели

Система автоматически определяет части модели по ключевым словам в именах mesh-объектов:

| Ключ textureScheme | Ключевые слова в имени mesh | Описание |
|--------------------|---------------------------|----------|
| `body` | body, корпус | Основной корпус шкафа |
| `door` | door, дверь, дверца | Дверца шкафа |
| `panel` | panel, панель | Монтажная панель |
| `insulation` | insulation, изоляция | Изоляционный слой |
| `insulationFrame` | insulation_frame, изоляция_рамка | Рамка изоляции |
| `dinRail` | din_rail, din, rail, рейка | DIN-рейки |

## Создание текстурных наборов

### Шаг 1: Подготовка текстур

1. Создайте папку для материала: `public/assets/textures/metal/brushed/`
2. Поместите текстуры с правильными суффиксами:
   ```
   brushed_albedo.jpg       (обязательно!)
   brushed_normal.jpg       (рекомендуется)
   brushed_roughness.jpg    (рекомендуется)
   brushed_ao.jpg           (опционально)
   brushed_metalness.jpg    (для металлов)
   ```

### Шаг 2: Оптимизация

- Используйте **JPG** с качеством 85-90%
- Убедитесь что текстуры **бесшовные** (tileable)
- Используйте **степени двойки** для размеров (512, 1024, 2048)

### Шаг 3: Применение

```javascript
// В конфигураторе при загрузке шкафа
const cabinet = new CabinetModel(modelPath, {
    // ... другие параметры ...
    textureScheme: {
        body: '/assets/textures/metal/brushed',
        door: '/assets/textures/metal/brushed'
    }
});
```

## Источники бесплатных PBR-текстур

1. **Poly Haven** (https://polyhaven.com/textures)
   - Лицензия: CC0 (полностью бесплатно)
   - Высокое качество, PBR
   
2. **ambientCG** (https://ambientcg.com/)
   - Лицензия: CC0
   - Большая коллекция материалов
   
3. **3D Textures** (https://3dtextures.me/)
   - Лицензия: CC0
   - Хорошие металлы и дерево

## Производительность

### Лучшие практики:
✅ Используйте 1024×1024 для большинства случаев
✅ Применяйте текстуры только там, где видна разница
✅ Используйте JPG вместо PNG (если нет прозрачности)
✅ Группируйте объекты с одинаковыми текстурами

### Избегайте:
❌ Текстур 4K+ без необходимости
❌ Дублирования текстур (переиспользуйте!)
❌ Слишком много различных текстур в одной сцене
❌ Несжатых PNG для непрозрачных поверхностей

## Пример: Металлический шкаф

```javascript
// Реалистичный металлический шкаф
const metalCabinet = new CabinetModel('/assets/models/cabinet.glb', {
    type: 'floor',
    width: 700,
    height: 500,
    depth: 240,
    name: 'Металлический шкаф',
    // Цвета как fallback (если текстуры не загрузятся)
    colorScheme: {
        default: 0x808080,
        body: 0x808080,
        door: 0x909090,
        panel: 0xC0C0C0
    },
    // Текстуры (приоритет над цветами)
    textureScheme: {
        body: '/assets/textures/metal/brushed_steel',
        door: '/assets/textures/metal/painted_gray',
        panel: '/assets/textures/metal/galvanized',
        dinRail: '/assets/textures/metal/zinc'
    }
});
```

## Отладка текстур

### Проверка загрузки:
1. Откройте консоль браузера (F12)
2. Найдите логи `✅ Текстура загружена:` или `⚠️ Текстура не найдена:`
3. Проверьте, что пути правильные

### Если текстуры не видны:
- Проверьте пути (без суффикса `_albedo` в параметре!)
- Убедитесь, что файлы существуют
- Проверьте права доступа к папке `public/assets/textures/`
- Очистите кеш браузера (Ctrl+Shift+R)

### Проверка в сети:
```
DevTools → Network → Img → Проверьте статус загрузки
200 OK — текстура загружена
404 Not Found — файл не найден (проверьте путь)
```

## API Reference

### SceneManager.loadPBRTextures(basePath)
Загружает набор PBR-текстур.

**Параметры:**
- `basePath` (string) — путь БЕЗ суффикса, например `/assets/textures/metal/brushed`

**Возвращает:**
- `Promise<Object>` — объект с загруженными текстурами (`map`, `normalMap`, `roughnessMap`, `aoMap`, `metalnessMap`)

**Пример:**
```javascript
const textures = await sceneManager.loadPBRTextures('/assets/textures/metal/brushed');
// textures.map — базовая текстура
// textures.normalMap — карта нормалей
// и т.д.
```

### configurator.changeCabinetTexture(cabinetId, partName, texturePath)
Изменяет текстуру части шкафа.

**Параметры:**
- `cabinetId` (string) — ID шкафа
- `partName` (string) — название части ('body', 'door', 'panel', и т.д.)
- `texturePath` (string) — путь к текстуре БЕЗ суффикса

**Пример:**
```javascript
await configurator.changeCabinetTexture(
    'cabinet_123...',
    'body',
    '/assets/textures/metal/chrome'
);
```

---

## Быстрый старт

1. **Создайте папку**: `public/assets/textures/metal/brushed/`
2. **Добавьте текстуру**: `brushed_albedo.jpg` (минимум)
3. **Примените**:
   ```javascript
   textureScheme: {
       body: '/assets/textures/metal/brushed'
   }
   ```
4. **Проверьте** в консоли: `✅ Текстура загружена: ...`
5. **Готово!** Шкаф теперь с текстурой 🎉
