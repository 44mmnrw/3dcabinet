# Workflow материалов и текстур — Рекомендации для 3Cabinet

## Сравнение подходов

### Подход 1: Базовые материалы + динамическая окраска (JS)
**Что**: Экспортировать модель с "пустыми" серыми материалами, раскрашивать через Three.js

**✅ Преимущества**:
- **Малый размер файлов** (~100-200 КБ вместо 500+ КБ с текстурами)
- **Динамическая окраска** — пользователь может выбирать цвет в UI
- **Простая кастомизация** — нет нужды экспортировать 10 вариантов GLB для каждого цвета
- **Быстрая загрузка** — одна модель, цвета применяются мгновенно
- **Единый стиль** — все шкафы окрашены одинаково (PBR-материалы)

**❌ Недостатки**:
- **Ограниченная реалистичность** — нет фото-текстур (царапины, потёртости, логотипы)
- **Нет сложных материалов** — только цвет + metalness/roughness
- **Выглядит "пластиково"** без текстур

**Когда использовать**:
- ✅ **Конфигуратор с выбором цвета** (главная фича 3Cabinet!)
- ✅ Шкафы, где важна **скорость загрузки** (10+ моделей на сцене)
- ✅ Каталог оборудования с **унифицированным стилем**

---

### Подход 2: Готовые материалы/текстуры из KeyShot
**Что**: Экспортировать GLB с полными текстурами (Albedo, Normal, Metalness, Roughness, AO)

**✅ Преимущества**:
- **Фотореализм** — выглядит как в KeyShot (царапины, отражения, детали)
- **Сложные материалы** — кожа, дерево, текстиль, логотипы, надписи
- **Готовая визуализация** — не нужно настраивать освещение в Three.js

**❌ Недостатки**:
- **Большой размер** — 2-5 МБ на модель (даже со сжатием DRACO ~500 КБ - 1 МБ)
- **Нет динамической окраски** — цвет "запечён" в текстуры
- **Долгая загрузка** — 5 моделей = 5-10 МБ трафика
- **Множество вариантов** — для 5 цветов нужно 5 разных GLB-файлов

**Когда использовать**:
- ✅ **Презентационные модели** (детальный просмотр готового проекта)
- ✅ Оборудование с **брендингом** (логотипы Dell, HP, APC на серверах)
- ✅ **Hero-модели** для маркетинга (один идеальный шкаф для лендинга)

---

## Рекомендуемый Workflow для 3Cabinet

### 🎯 Гибридный подход (лучшее из двух миров)

**Для шкафов** (основные объекты с выбором цвета):
1. **Экспорт из KeyShot**:
   - Материал: Simple Paint (без текстур)
   - Цвет: Серый #808080 (нейтральный базовый)
   - Metalness: 0.2-0.3
   - Roughness: 0.6-0.7
   - **НЕТ текстур** (Albedo/Normal/AO отключены)
   
2. **В Three.js** (CabinetModel.js):
   - Применяем цвет через `applyPartColors()` или единый цвет
   - Пользователь меняет цвет → просто `material.color.set(newColor)`
   - Размер GLB: **~100-200 КБ**

**Для оборудования** (декоративное, без кастомизации):
1. **Экспорт из KeyShot**:
   - Материалы: С текстурами (брендинг, надписи, реализм)
   - Draco Compression: ✅ Enabled
   - Размер GLB: **~500 КБ - 1 МБ** (сжато)
   
2. **В Three.js**:
   - Загружаем "как есть" с материалами KeyShot
   - Применяем коррекцию цветового пространства (convertLinearToSRGB)
   - **НЕ перекрашиваем** — сохраняем оригинальный вид

---

## Пример: Структура материалов

### Базовая модель шкафа (для окраски)

**KeyShot Material Setup**:
```
Cabinet Body:
├── Material: Metal → Simple Paint
├── Color: #808080 (средне-серый)
├── Metalness: 0.3
├── Roughness: 0.7
└── Textures: NONE (отключить Albedo, Normal, AO)

Door:
├── Material: Metal → Simple Paint
├── Color: #999999 (светло-серый)
├── Metalness: 0.2
├── Roughness: 0.6
└── Textures: NONE

DIN Rails:
├── Material: Metal → Brushed Aluminum
├── Color: #C0C0C0 (алюминий)
├── Metalness: 0.8
├── Roughness: 0.4
└── Textures: NONE (или простая Roughness map)
```

**GLB Export Settings**:
- Format: GLB Binary
- Units: Millimeters
- Draco Compression: ✅ Enabled
- Textures: ❌ Do NOT embed (только цвет)
- Result: **~150 КБ**

**Three.js (динамическая окраска)**:
```javascript
// CabinetModel.js
applyPartColors() {
    const scheme = this.config.colorScheme;
    
    this.model.traverse((child) => {
        if (child.isMesh) {
            if (child.name.includes('body')) {
                child.material.color.set(scheme.body);  // ← Динамический цвет
            } else if (child.name.includes('door')) {
                child.material.color.set(scheme.door);
            }
            child.material.needsUpdate = true;
        }
    });
}

// Пользователь выбрал новый цвет
changeColor(newColor) {
    this.model.traverse((child) => {
        if (child.isMesh && child.name.includes('body')) {
            child.material.color.set(newColor);  // ← Мгновенное изменение!
        }
    });
}
```

---

### Детальная модель оборудования (с текстурами)

**KeyShot Material Setup**:
```
Server Dell PowerEdge:
├── Material: Metal → Brushed Aluminum
├── Color: #1A1A1A (тёмно-серый)
├── Metalness: 0.7
├── Roughness: 0.5
├── Textures:
│   ├── Albedo: dell_logo.png (лого на передней панели)
│   ├── Normal: brushed_metal_normal.png (микротекстура)
│   ├── Roughness: scratches_roughness.png (царапины)
│   └── AO: server_ao.png (тени в щелях)
└── Export size: ~2 МБ → DRACO: ~600 КБ
```

**GLB Export Settings**:
- Format: GLB Binary
- Units: Millimeters
- Draco Compression: ✅ Enabled
- Textures: ✅ Embed all (PNG, max 2048x2048)
- Result: **~600 КБ** (DRACO compressed)

**Three.js (без изменений)**:
```javascript
// configurator.js
async loadEquipment() {
    const gltf = await loader.loadAsync('server_dell.glb');
    const model = gltf.scene;
    
    // Коррекция цветового пространства KeyShot → Three.js
    model.traverse((child) => {
        if (child.isMesh && child.material) {
            if (child.material.color) {
                child.material.color.convertLinearToSRGB();  // ← Яркость +30%
            }
            if (child.material.map) {
                child.material.map.colorSpace = THREE.SRGBColorSpace;
            }
            child.material.needsUpdate = true;
        }
    });
    
    cabinet.addEquipment(model, railIndex);
    // Цвет НЕ меняем — пользователь видит реалистичный сервер Dell
}
```

---

## UI для выбора цвета

### HTML (Color Picker)

```html
<!-- configurator/index.blade.php -->
<div class="color-picker-panel">
    <h3>Цвет шкафа</h3>
    <div class="color-swatches">
        <button class="swatch" data-color="#2196F3" style="background: #2196F3"></button>
        <button class="swatch" data-color="#F44336" style="background: #F44336"></button>
        <button class="swatch" data-color="#4CAF50" style="background: #4CAF50"></button>
        <button class="swatch" data-color="#FF9800" style="background: #FF9800"></button>
        <button class="swatch" data-color="#9E9E9E" style="background: #9E9E9E"></button>
    </div>
    
    <!-- Или кастомный цвет -->
    <input type="color" id="custom-color-picker" value="#2196F3">
</div>
```

### JavaScript (Изменение цвета)

```javascript
// configurator.js
setupColorPicker() {
    const swatches = document.querySelectorAll('.swatch');
    const customPicker = document.getElementById('custom-color-picker');
    
    swatches.forEach(swatch => {
        swatch.addEventListener('click', () => {
            const color = swatch.dataset.color;
            this.changeCabinetColor(color);
        });
    });
    
    customPicker.addEventListener('input', (e) => {
        this.changeCabinetColor(e.target.value);
    });
}

changeCabinetColor(hexColor) {
    const cabinet = this.cabinetManager.getSelectedCabinet();
    if (!cabinet) return;
    
    cabinet.model.traverse((child) => {
        if (child.isMesh) {
            // Раскрашиваем только body и door, оставляем DIN-рейки серыми
            if (child.name.includes('body') || child.name.includes('door')) {
                child.material.color.set(hexColor);
                child.material.needsUpdate = true;
            }
        }
    });
    
    console.log(`✅ Цвет шкафа изменён на ${hexColor}`);
}
```

---

## Производительность

### Сравнение размеров (один шкаф 700×500×240)

| Подход | Размер GLB | Текстуры | Загрузка | Динамический цвет |
|--------|------------|----------|----------|-------------------|
| **Базовый материал** | 150 КБ | Нет | ~50 мс | ✅ Да |
| **KeyShot без текстур** | 180 КБ | Нет | ~70 мс | ✅ Да (через JS) |
| **KeyShot с текстурами** | 600 КБ | Да (embedded) | ~200 мс | ❌ Нет |
| **KeyShot полный** | 2-5 МБ | Да (4K) | ~800 мс | ❌ Нет |

### Загрузка сцены (5 шкафов + 10 единиц оборудования)

| Подход | Общий размер | Время загрузки | Память GPU |
|--------|--------------|----------------|------------|
| **Гибридный** (базовые шкафы + текстурное оборудование) | **1.5 МБ** | ~500 мс | ~200 МБ |
| **Только базовые** (всё без текстур) | **800 КБ** | ~300 мс | ~100 МБ |
| **Только KeyShot** (всё с текстурами) | **8-10 МБ** | ~3 сек | ~800 МБ |

---

## Рекомендации по экспорту

### Для шкафов (кастомизируемые)

**KeyShot Workflow**:
1. Создать материал "Base Cabinet Paint":
   ```
   Material: Metal → Simple Paint
   Color: #999999 (нейтральный серый)
   Metalness: 0.2-0.3
   Roughness: 0.6-0.7
   ```
2. Применить ко всем частям (body, door, panel)
3. DIN-рейки: отдельный материал "Aluminum" (metalness 0.8)
4. Export:
   - GLB Binary
   - Millimeters
   - Draco Compression: ✅
   - Textures: ❌ Do NOT embed

**Результат**: ~150 КБ, полностью кастомизируемый через Three.js

### Для оборудования (реалистичное)

**KeyShot Workflow**:
1. Создать фото-реалистичные материалы:
   ```
   Server Body: Brushed Aluminum + Normal map
   Front Panel: Plastic + Logo decal (Albedo texture)
   LEDs: Emissive material (светятся)
   ```
2. Export:
   - GLB Binary
   - Millimeters
   - Draco Compression: ✅
   - Textures: ✅ Embed (PNG, max 2048×2048)

**Результат**: ~600 КБ, фото-реалистичный, НЕ кастомизируемый

---

## Частые проблемы

### Проблема 1: Модель из KeyShot слишком тёмная

**Причина**: Linear color space (KeyShot) vs sRGB (Three.js)

**Решение** (в CabinetModel.js):
```javascript
model.traverse((child) => {
    if (child.isMesh && child.material.color) {
        child.material.color.convertLinearToSRGB();  // ← Яркость +30%
        child.material.needsUpdate = true;
    }
});
```

### Проблема 2: Цвет меняется, но выглядит "плоско"

**Причина**: Не настроены metalness/roughness

**Решение**:
```javascript
changeCabinetColor(hexColor) {
    cabinet.model.traverse((child) => {
        if (child.isMesh && child.material) {
            child.material.color.set(hexColor);
            child.material.metalness = 0.3;  // ← Добавить металличность
            child.material.roughness = 0.7;  // ← Добавить шероховатость
            child.material.needsUpdate = true;
        }
    });
}
```

### Проблема 3: Текстуры KeyShot не загружаются

**Причина**: Пути к текстурам относительные, файлы не найдены

**Решение**: Embed текстуры в GLB при экспорте:
- KeyShot: Export → Textures → ✅ Embed in GLB (не Use External Files)

---

## Итоговая рекомендация для 3Cabinet

### ✅ Используйте **Гибридный подход**:

1. **Шкафы** (TSH, настенные, напольные):
   - Базовые материалы без текстур (~150 КБ)
   - Динамическая окраска через `applyPartColors()`
   - UI с color picker для выбора цвета
   - **Реализовано**: метод `changeColor(hexColor)` в CabinetModel

2. **Оборудование** (серверы, коммутаторы, ИБП):
   - Полные материалы KeyShot с текстурами (~600 КБ)
   - Коррекция цветового пространства при загрузке
   - **НЕ кастомизируемые** — пользователь видит реалистичное оборудование

3. **Аксессуары** (кабели, винты, заглушки):
   - Простые Three.js геометрии (BoxGeometry, CylinderGeometry)
   - Процедурные материалы (без GLB-файлов!)
   - **Минимальный размер** (~0 КБ, создаются в коде)

### Пример структуры файлов:

```
public/assets/models/
├── thermocabinets/          # Шкафы (базовые материалы, ~150 КБ каждый)
│   ├── tsh_700_500_240.glb  # 150 КБ, серый материал
│   ├── tsh_1000_700_400.glb
│   └── wall_600_400_200.glb
│
├── equipment/               # Оборудование (с текстурами, ~600 КБ каждый)
│   ├── servers/
│   │   ├── dell_r740.glb    # 650 КБ, лого Dell + текстуры
│   │   └── hp_dl380.glb     # 620 КБ, лого HP + текстуры
│   ├── switches/
│   │   └── cisco_2960.glb   # 580 КБ, брендинг Cisco
│   └── power/
│       └── apc_ups.glb      # 700 КБ, надписи APC
│
└── accessories/             # НЕТ! Создаются в коде через Three.js GeometryUtils
    # (кабели, винты — процедурная генерация)
```

**Общий размер** для типичной сцены:
- 3 шкафа: 450 КБ
- 6 единиц оборудования: 3.6 МБ
- **Итого: ~4 МБ** (приемлемо для web, загружается ~1-2 сек)

---

**Версия**: 1.0  
**Дата**: 9 ноября 2025  
**Автор**: 3Cabinet Team  
**Связано**: DRACO_COMPRESSION_GUIDE.md, GLB_MODEL_GUIDE.md
