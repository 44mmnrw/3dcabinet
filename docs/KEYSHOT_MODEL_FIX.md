# 🔧 Исправление темных внутренностей в GLB-моделях из KeyShot

## Проблема

Модели, экспортированные из KeyShot в формат GLB/GLTF, имеют **очень темные внутренние поверхности** (почти черные), из-за чего они не освещаются корректно в Three.js.

**Причины:**
1. KeyShot экспортирует материалы с базовым цветом близким к черному (RGB ~0.05)
2. Внутренние поверхности не получают достаточно ambient light
3. Материалы настроены для рендеринга в KeyShot, а не для real-time движков

---

## ✅ Решение 1: Изменить материалы в KeyShot (перед экспортом)

### Шаг 1: Выделить внутренние части модели

В KeyShot:
1. Переключитесь в режим **Scene** (вкладка слева)
2. Найдите внутренние части (обычно называются `interior`, `inner_walls`, `back_panel` и т.д.)
3. Выделите их (Ctrl+Click для множественного выбора)

### Шаг 2: Изменить цвет материала

В панели **Material**:
1. Откройте материал внутренних частей
2. Найдите параметр **Color** или **Diffuse Color**
3. Установите светлый серый цвет:
   - RGB: `(180, 180, 180)` или HEX: `#B4B4B4`
   - Яркость (Brightness): **70-80%**
4. Если используется текстура — отключите её для внутренностей

### Шаг 3: Настройки экспорта GLB

**File → Export → glTF 2.0 (.glb/.gltf)**

Важные настройки:
```
✅ Export Type: GLB (binary)
✅ Material Type: PBR (Metallic-Roughness) — не Specular-Glossiness!
✅ Color Space: sRGB — НЕ Linear!
✅ Lighting: Baked — если есть такая опция
✅ Compression: Draco (если нужно сжатие)

❌ НЕ включать: 
   - Export Lighting
   - Export Cameras
   - Export Animations (если не нужны)
```

### Шаг 4: Проверка в glTF Viewer

Перед импортом в проект:
1. Откройте модель в https://gltf-viewer.donmccurdy.com/
2. Включите **Environment: Studio** (справа сверху)
3. Проверьте что внутренности видны и освещены

---

## ✅ Решение 2: Исправить материалы в Blender

Если модель уже экспортирована и нужно быстро исправить:

### Шаг 1: Импортировать GLB в Blender

1. **File → Import → glTF 2.0 (.glb/.gltf)**
2. Выберите файл `tsh_700_500_240.glb`
3. Нажмите **Import glTF 2.0**

### Шаг 2: Найти внутренние части

В режиме **Edit Mode** (Tab):
1. **Outliner** (справа сверху) → найдите объекты с именами типа:
   - `interior`
   - `inner_walls`
   - `back_panel`
   - `mounting_panel`
2. Или выделите все: `A` → `P` → **By Material** (разделить по материалам)

### Шаг 3: Изменить материалы внутренних частей

Переключитесь в **Shading** workspace (вкладка вверху):

1. Выделите внутренний mesh
2. В **Shader Editor** (снизу) найдите узел **Principled BSDF**
3. Измените параметры:
   ```
   Base Color: RGB (0.7, 0.7, 0.7) — светло-серый, НЕ черный!
   Metallic: 0.0 — НЕ металл (важно!)
   Roughness: 0.8 — матовая поверхность
   Specular: 0.5 — умеренные блики
   ```

### Шаг 4: Проверить нормали

В **Edit Mode** (Tab):
1. Выделите все внутренние грани: `A`
2. Включите отображение нормалей:
   - **Overlay** (справа сверху) → **Normals** → включите **Face Normals**
3. Проверьте что синие линии (нормали) направлены **внутрь шкафа**
4. Если нормали инвертированы:
   - **Mesh → Normals → Flip** (или `Alt+N` → Flip)

### Шаг 5: Убрать двойные грани (если есть)

Иногда KeyShot создаёт двойные стенки:

1. В **Edit Mode**: выделите всё (`A`)
2. **Mesh → Clean Up → Merge by Distance** (или `M`)
3. Установите **Distance**: `0.0001`
4. Нажмите **OK**

### Шаг 6: Экспортировать обратно в GLB

**File → Export → glTF 2.0 (.glb/.gltf)**

Настройки экспорта:
```
Format: glTF Binary (.glb)

Include:
  ✅ Selected Objects (если выбраны нужные)
  ✅ Apply Modifiers
  ✅ UVs
  ✅ Normals
  ✅ Vertex Colors (если нужны)

Transform:
  ✅ +Y Up (важно для Three.js!)

Geometry:
  ✅ Compression: Draco (опционально, уменьшает размер)
  
Material:
  ✅ Materials: Export

Animation:
  ❌ (отключить если не нужна)
```

---

## ✅ Решение 3: Добавить Emission к внутренним материалам (quick fix)

Если нужно срочное решение — добавить самосвечение:

### В KeyShot (перед экспортом):
1. Материал внутренних частей → **Emission**
2. Установите:
   - **Emission Color**: белый `#FFFFFF`
   - **Emission Intensity**: `0.3-0.5` (не слишком яркий!)

### В Blender (после импорта):
1. Узел **Principled BSDF** внутренних частей
2. **Emission Color**: RGB `(1, 1, 1)` — белый
3. **Emission Strength**: `0.5` (регулируйте по вкусу)

**Минус этого метода**: внутренности будут светиться даже при закрытой двери.

---

## ✅ Решение 4: Использовать Light Probes в Three.js (код)

Если исправить модель невозможно, можно добавить **LightProbe** для равномерного освещения:

```javascript
// В SceneManager.js, после setupLighting():

// Создать light probe для глобального освещения
const lightProbe = new THREE.LightProbe();
this.scene.add(lightProbe);

// Установить цвет и интенсивность
const probeIntensity = 1.5;
lightProbe.sh.coefficients[0].set(
    probeIntensity, 
    probeIntensity, 
    probeIntensity
);

console.log('💡 LightProbe добавлен для равномерного освещения');
```

---

## 🎯 Рекомендуемый порядок действий

### Вариант A: Быстрое исправление (5 минут)
1. Открыть `tsh_700_500_240.glb` в **Blender**
2. Найти внутренние части (через Outliner)
3. Изменить Base Color на светло-серый (0.7, 0.7, 0.7)
4. Экспортировать обратно в GLB
5. Заменить файл в `public/assets/models/...`
6. Обновить страницу (Ctrl+Shift+R)

### Вариант B: Правильное исправление (30 минут)
1. Открыть исходный файл в **KeyShot**
2. Изменить материалы внутренних частей (светло-серые)
3. Настроить правильный экспорт GLB (sRGB, PBR)
4. Проверить в glTF Viewer
5. Заменить файл в проекте
6. Тестировать в браузере

---

## 📊 Сравнение моделей

### Модель из Blender (_tsh_700_500_240.glb) — ✅ Работает
```
Размер: 265 KB
Материалы: MeshStandardMaterial
Base Color: RGB (0.8, 0.8, 0.8) — светлый!
Металличность: 0.3
Шероховатость: 0.7
Нормали: правильные (внутрь)
```

### Модель из KeyShot (tsh_700_500_240.glb) — ❌ Темная
```
Размер: 1024 KB (4x больше!)
Материалы: MeshStandardMaterial
Base Color: RGB (0.05, 0.05, 0.05) — почти черный!
Металличность: 0.0-0.3
Шероховатость: 0.4-0.8
Проблема: темные базовые цвета
```

---

## 🔍 Диагностика проблемы

### В браузере (DevTools):

```javascript
// Проверить цвета материалов
const cabinet = app.cabinetManager.cabinets[0];
cabinet.model.traverse((child) => {
    if (child.isMesh && child.material) {
        const mat = Array.isArray(child.material) ? child.material[0] : child.material;
        const brightness = (mat.color.r + mat.color.g + mat.color.b) / 3;
        console.log(`${child.name}: brightness=${brightness.toFixed(2)}, color=`, mat.color);
    }
});
```

**Нормальные значения:** brightness > 0.5 (светлые материалы)  
**Проблемные значения:** brightness < 0.3 (темные — не видны внутри)

---

## 📚 Полезные ссылки

**Инструменты:**
- [glTF Viewer](https://gltf-viewer.donmccurdy.com/) — проверка моделей онлайн
- [Blender](https://www.blender.org/) — бесплатный 3D редактор
- [Three.js Sandbox](https://threejs.org/editor/) — быстрая проверка GLB

**Документация:**
- [glTF 2.0 Specification](https://registry.khronos.org/glTF/specs/2.0/glTF-2.0.html)
- [Three.js MeshStandardMaterial](https://threejs.org/docs/#api/en/materials/MeshStandardMaterial)
- [KeyShot glTF Export](https://manual.keyshot.com/manual/export-gltf/)

---

## ✅ Итог

**Проблема**: KeyShot экспортирует материалы с очень темными базовыми цветами → внутренности черные.

**Решение**: Изменить Base Color внутренних материалов на светло-серый (RGB 0.7+) **в самой модели**, а не в коде.

**Результат**: Внутренности будут правильно освещаться стандартным Three.js освещением без хаков в коде.
