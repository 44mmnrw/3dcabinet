# 🎨 Справочник по управлению цветами шкафов

## Где находится код цветов

### 📍 Файл: `public/js/pages/configurator.js` (строки 30-45)

```javascript
// ====== ЦВЕТОВАЯ СХЕМА ШКАФОВ (HEX-цвета) ======
this.cabinetColorScheme = {
    // Цвет по умолчанию
    default: 0x673831,           // ← Коричневый
    
    // ОСНОВНЫЕ ЧАСТИ:
    body: 0x673831,              // ← Корпус/каркас шкафа
    door: 0x673831,              // ← Дверца шкафа
    panel: 0x673831,             // ← Монтажная панель
    
    // ИЗОЛЯЦИЯ:
    insulation: 0xE8E8E8,        // ← Изоляционный материал (светло-серый)
    insulationFrame: 0xC0C0C0,   // ← Рамка изоляции (серебристый)
    
    // МОНТАЖ:
    dinRail: 0xA8A8A8            // ← DIN-рейки (темно-серый)
};
```

---

## 🎨 Популярные цвета (готовые значения)

### Серые оттенки (металлы):
```javascript
0x000000  // Чёрный
0x2C2C2C  // Антрацит (тёмно-серый)
0x555555  // Графит
0x808080  // Средне-серый
0xA8A8A8  // Светло-серый
0xC0C0C0  // Серебристый
0xD3D3D3  // Светлый металлик
0xFFFFFF  // Белый
```

### Металлы:
```javascript
0xC0C0C0  // Серебро
0xFFD700  // Золото
0xB87333  // Медь
0x708090  // Сталь
0x4A4A4A  // Оксидированный металл
```

### RAL-подобные промышленные цвета:
```javascript
0x3B3C3E  // RAL 7021 (Чёрно-серый)
0x6C7156  // RAL 7003 (Серо-зелёный мох)
0xFFFFFF  // RAL 9003 (Сигнальный белый)
0xF6F6F6  // RAL 9016 (Транспортный белый)
0xE5E5E5  // RAL 9018 (Папирусно-белый)
```

### Цветные варианты (для визуализации):
```javascript
0xFF0000  // Красный
0x00FF00  // Зелёный
0x0000FF  // Синий
0xFFFF00  // Жёлтый
0xFF00FF  // Пурпурный
0x00FFFF  // Голубой
```

---

## 📝 Примеры изменения цветов

### Пример 1: Классический серый металлический шкаф
```javascript
this.cabinetColorScheme = {
    default: 0x808080,           // Средне-серый
    body: 0x555555,              // Тёмный графит (корпус)
    door: 0x555555,              // Тёмный графит (дверь)
    panel: 0xC0C0C0,             // Светлый металлик (панель)
    insulation: 0xFFFFFF,        // Белая изоляция
    insulationFrame: 0xC0C0C0,   // Серебристая рамка
    dinRail: 0x708090            // Стальные рейки
};
```

### Пример 2: Чёрный премиум-шкаф
```javascript
this.cabinetColorScheme = {
    default: 0x000000,           // Чёрный
    body: 0x000000,              // Чёрный корпус
    door: 0x000000,              // Чёрная дверь
    panel: 0x2C2C2C,             // Антрацит (панель)
    insulation: 0x555555,        // Тёмно-серая изоляция
    insulationFrame: 0x808080,   // Серая рамка
    dinRail: 0x4A4A4A            // Тёмно-серые рейки
};
```

### Пример 3: Белый медицинский/лабораторный
```javascript
this.cabinetColorScheme = {
    default: 0xFFFFFF,           // Белый
    body: 0xF6F6F6,              // Почти белый (корпус)
    door: 0xFFFFFF,              // Чисто белый (дверь)
    panel: 0xE5E5E5,             // Светло-серый (панель)
    insulation: 0xFFFFFF,        // Белая изоляция
    insulationFrame: 0xC0C0C0,   // Серебристая рамка
    dinRail: 0xA8A8A8            // Светло-серые рейки
};
```

### Пример 4: Тёплые коричневые тона (текущий)
```javascript
this.cabinetColorScheme = {
    default: 0x673831,           // Коричневый
    body: 0x673831,              // Коричневый корпус
    door: 0x673831,              // Коричневая дверь
    panel: 0x8B4513,             // Тёмно-коричневая панель
    insulation: 0xE8E8E8,        // Светло-серая изоляция
    insulationFrame: 0xC0C0C0,   // Серебристая рамка
    dinRail: 0xA8A8A8            // Серые рейки
};
```

### Пример 5: Яркая визуализация (для отладки частей)
```javascript
this.cabinetColorScheme = {
    default: 0x808080,           // Серый
    body: 0xFF0000,              // КРАСНЫЙ корпус (хорошо видно!)
    door: 0x00FF00,              // ЗЕЛЁНАЯ дверь (отличается!)
    panel: 0x0000FF,             // СИНЯЯ панель (контрастно!)
    insulation: 0xFFFF00,        // ЖЁЛТАЯ изоляция (ярко!)
    insulationFrame: 0xFF00FF,   // ПУРПУРНАЯ рамка (выделяется!)
    dinRail: 0x00FFFF            // ГОЛУБЫЕ рейки (заметно!)
};
```

---

## 🔧 Как применить изменения

### Способ 1: Изменить в коде (постоянно)

1. Откройте файл: `public/js/pages/configurator.js`
2. Найдите строку 30: `this.cabinetColorScheme = {`
3. Измените нужные цвета (например, `body: 0xFF0000,`)
4. Сохраните файл
5. Обновите страницу (Ctrl+R или F5)

### Способ 2: Через консоль браузера (временно)

```javascript
// 1. Откройте консоль браузера (F12)

// 2. Измените цвет конкретной части:
configurator.cabinetColorScheme.body = 0xFF0000;  // Красный корпус

// 3. Пересоздайте шкаф для применения:
// (Удалите старый и создайте новый)
```

### Способ 3: Динамически через API (для будущего UI)

```javascript
// Создать метод для динамического изменения цвета части:
configurator.changePartColor = function(cabinetId, partName, newColor) {
    const cabinet = this.cabinetManager.getCabinetById(cabinetId);
    if (!cabinet) return;
    
    // Обновить цвет в схеме
    cabinet.config.colorScheme[partName] = newColor;
    
    // Применить заново
    cabinet.applyPartColors();
};

// Использование:
configurator.changePartColor(
    configurator.getCabinets()[0].id,  // ID шкафа
    'body',                             // Название части
    0xFF0000                            // Новый цвет (красный)
);
```

---

## 🔍 Как узнать, какие части есть в модели

Откройте консоль браузера (F12) и выполните:

```javascript
// Показать все mesh-объекты модели:
const cabinet = configurator.getCabinets()[0];
cabinet.model.traverse((child) => {
    if (child.isMesh) {
        console.log(`Mesh: ${child.name}`);
    }
});
```

Вы увидите список типа:
```
Mesh: body_001
Mesh: door_left
Mesh: panel_mounting
Mesh: insulation_front
Mesh: insulation_frame_top
Mesh: din_rail_1
```

Теперь вы знаете, какие имена использует ваша модель!

---

## 🎯 Быстрая шпаргалка

| Часть | Ключ в схеме | Где применяется | Пример цвета |
|-------|--------------|-----------------|--------------|
| Корпус/каркас | `body` | Основная металлическая конструкция | `0x555555` (графит) |
| Дверца | `door` | Передняя часть с петлями | `0x000000` (чёрный) |
| Монтажная панель | `panel` | Внутренняя панель для крепежа | `0xC0C0C0` (серебро) |
| Изоляция | `insulation` | Теплоизоляция/прокладки | `0xFFFFFF` (белый) |
| Рамка изоляции | `insulationFrame` | Металлическая окантовка | `0xC0C0C0` (серебро) |
| DIN-рейки | `dinRail` | Монтажные рейки | `0xA8A8A8` (серый) |
| По умолчанию | `default` | Неопознанные части | `0x808080` (серый) |

---

## 🌈 Конвертация цветов

### Из RGB в HEX:
```
RGB(255, 0, 0) → 0xFF0000
RGB(128, 128, 128) → 0x808080
RGB(255, 255, 255) → 0xFFFFFF
```

### Онлайн-инструменты:
- https://www.color-hex.com/ — цветовые схемы
- https://coolors.co/ — генератор палитр
- https://www.ralcolorchart.com/ — RAL цвета

### Формула конвертации:
```javascript
// RGB → HEX
const r = 255, g = 128, b = 64;
const hex = (r << 16) | (g << 8) | b;  // 0xFF8040

// HEX → RGB
const hex = 0xFF8040;
const r = (hex >> 16) & 0xFF;  // 255
const g = (hex >> 8) & 0xFF;   // 128
const b = hex & 0xFF;          // 64
```

---

## 💡 Советы

1. **Используйте цвета, близкие к реальным материалам** — так визуализация будет реалистичнее
2. **Избегайте слишком ярких цветов** (кроме отладки) — чистый красный/синий выглядят неестественно
3. **Текстуры важнее цветов** — для финального вида добавьте текстуры металла
4. **Тестируйте на разном освещении** — один цвет может выглядеть по-разному

---

**Готово! Теперь вы можете легко изменять цвета любых деталей шкафа!** 🎉
