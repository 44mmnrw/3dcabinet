# 🔦 Освещение внутренностей модели

## Проблема
**Внутренняя часть шкафа (открытая дверь) остаётся чёрной**, потому что:
1. DirectionalLight (ключевой, заполняющий, контровый) светит **извне**
2. AmbientLight был слишком слабым (0.6)
3. Нет источников света, которые "проникают" внутрь геометрии

---

## ✅ Решение

### 1. Увеличен Ambient Light: 0.6 → 0.8
**Ambient Light** — глобальное равномерное освещение всех поверхностей (не зависит от направления).

```javascript
// public/js/modules/SceneManager.js (строка 400)
const ambientLight = new THREE.AmbientLight(0xffffff, 0.8);  // Было 0.6
```

**Эффект**: Базовая яркость всех поверхностей увеличена на +33%.

### 2. Добавлен Hemisphere Light (небо + земля)
**HemisphereLight** — симулирует естественное освещение:
- **Сверху** (небо, цвет `0xffffff` белый) — освещает верхние грани
- **Снизу** (земля, цвет `0x444444` серый) — освещает нижние грани (отражение от пола)

```javascript
// public/js/modules/SceneManager.js (строки 407-415)
const hemisphereLight = new THREE.HemisphereLight(
    0xffffff,  // Цвет неба (белый)
    0x444444,  // Цвет земли (темно-серый)
    0.4        // Интенсивность
);
hemisphereLight.position.set(0, 3000, 0);  // Сверху
this.scene.add(hemisphereLight);
```

**Эффект**: 
- Внутренние поверхности получают мягкое освещение сверху и снизу
- Создаётся объём (верхняя часть светлее нижней)
- Нет резких теней внутри шкафа

---

## 🎛️ GUI-контроль

### Добавлен слайдер "🔦 Interior Light"
**Расположение**: GUI → Lighting → 🔦 Interior Light

**Диапазон**: 0-2.0 (шаг 0.1)

**Назначение**: Управление интенсивностью HemisphereLight

```javascript
// public/js/modules/SceneManager.js (строки 590-592)
lightFolder.add(this.guiState, 'hemisphereIntensity', 0, 2, 0.1)
    .name('🔦 Interior Light')
    .onChange((value) => {
        if (this.hemisphereLight) this.hemisphereLight.intensity = value;
    });
```

**Рекомендуемые значения**:
| Значение | Эффект | Когда использовать |
|----------|--------|-------------------|
| **0.0** | Темные внутренности | Драматичное освещение |
| **0.4** | ✅ Нормальное (по умолчанию) | Естественное освещение |
| **0.8** | Яркие внутренности | Презентационный режим |
| **1.5** | Очень яркие | Технические чертежи |

---

## 🎭 Обновлены пресеты освещения

Все 5 пресетов теперь включают настройку hemisphereIntensity:

### 1. Мягкий (по умолчанию) ✅
```javascript
ambientIntensity: 0.8      // Было 0.6
hemisphereIntensity: 0.4   // НОВЫЙ параметр
```
**Эффект**: Мягкое освещение внутренностей, естественный вид.

### 2. Жёсткий (контрастный)
```javascript
ambientIntensity: 0.2
hemisphereIntensity: 0.1   // Минимум (тёмные внутренности)
```
**Эффект**: Высокий контраст, драматичные тени, внутренности в полутени.

### 3. Равномерный (студия)
```javascript
ambientIntensity: 0.7
hemisphereIntensity: 0.5   // Средний уровень
```
**Эффект**: Студийное освещение, хорошо видны все детали.

### 4. Драматичный (тёмный)
```javascript
ambientIntensity: 0.2
hemisphereIntensity: 0.0   // ВЫКЛЮЧЕН (максимальная темнота)
```
**Эффект**: Художественное освещение, внутренности почти чёрные.

### 5. Яркий (презентация)
```javascript
ambientIntensity: 0.8
hemisphereIntensity: 0.6   // Максимум (яркие внутренности)
```
**Эффект**: Презентационный режим, всё хорошо освещено и видно.

---

## Техническое объяснение

### HemisphereLight — как работает?

**Формула освещения**:
```
finalColor = (skyColor * dot(normal, up) + groundColor * dot(normal, down)) * intensity
```

**Простыми словами**:
- Если поверхность смотрит **вверх** (`normal` направлена к небу) → получает `skyColor` (белый)
- Если поверхность смотрит **вниз** (`normal` направлена к земле) → получает `groundColor` (серый)
- Боковые поверхности получают **смесь** двух цветов

**Пример**:
- **Верхняя стенка внутри шкафа** (normal вверх) → белый свет × 0.4 = светлая
- **Нижняя стенка внутри шкафа** (normal вниз) → серый свет × 0.4 = тёмная
- **Боковые стенки** (normal горизонтально) → смесь белого и серого = средняя яркость

### Почему это лучше чем просто Ambient?

**Ambient Light**:
```javascript
// Все поверхности получают ОДИНАКОВЫЙ цвет (плоско)
ambientLight = new THREE.AmbientLight(0xffffff, 0.8);
```
❌ Проблема: Нет объёма, всё одинаково яркое.

**Hemisphere Light**:
```javascript
// Верх светлее, низ темнее (объёмно)
hemisphereLight = new THREE.HemisphereLight(
    0xffffff,  // Верх (небо)
    0x444444,  // Низ (земля)
    0.4
);
```
✅ Преимущество: Создаёт градиент яркости → объём и реализм.

---

## Визуальное сравнение

### До (только Ambient 0.6)
```
Внутри шкафа: Чёрная область
Причина: Ambient слишком слабый, DirectionalLight не проникает внутрь
```

### После (Ambient 0.8 + Hemisphere 0.4)
```
Внутри шкафа: Видны внутренние поверхности, стойки, задняя стенка
Причина: Ambient даёт базовую яркость, Hemisphere создаёт объём
```

---

## Как проверить

### 1. Обновите страницу
```
Ctrl + Shift + R
```

### 2. Откройте модель с дверью
Убедитесь что внутренняя часть теперь **не чёрная**, а освещена.

### 3. Откройте GUI → Lighting
Найдите новый контрол:
```
Key Light    [========]
Fill Light   [======]
Rim Light    [==]
Ambient      [========]
🔦 Interior Light [====]  ← НОВЫЙ СЛАЙДЕР
```

### 4. Протестируйте слайдер
- **Установите 0.0** → внутренности должны потемнеть
- **Установите 1.0** → внутренности должны стать ярче
- **Установите 0.4** → вернуться к умолчанию

### 5. Протестируйте пресеты
**GUI → Lighting → Presets**:
- **Жёсткий (контрастный)** → внутренности тёмные (hemisphereIntensity = 0.1)
- **Яркий (презентация)** → внутренности яркие (hemisphereIntensity = 0.6)
- **Драматичный (тёмный)** → внутренности чёрные (hemisphereIntensity = 0.0)

---

## Дополнительные настройки

### Изменить цвет внутреннего освещения

Если нужен **тёплый свет** внутри (как от лампочки):

```javascript
// public/js/modules/SceneManager.js (строка 409)
const hemisphereLight = new THREE.HemisphereLight(
    0xfff4e6,  // Тёплый белый (было 0xffffff)
    0x444444,  // Земля остаётся серой
    0.4
);
```

Если нужен **холодный свет** (как от светодиодов):

```javascript
const hemisphereLight = new THREE.HemisphereLight(
    0xe6f7ff,  // Холодный белый
    0x444444,
    0.4
);
```

### Изменить цвет "земли" (отражение снизу)

Если пол **деревянный** (коричневый):

```javascript
const hemisphereLight = new THREE.HemisphereLight(
    0xffffff,
    0x8b6f47,  // Коричневый (было 0x444444)
    0.4
);
```

Если пол **белый** (плитка):

```javascript
const hemisphereLight = new THREE.HemisphereLight(
    0xffffff,
    0xcccccc,  // Светло-серый (было 0x444444)
    0.4
);
```

---

## Альтернативные решения (не реализованы)

### Вариант 1: PointLight внутри шкафа
**Плюсы**: Реалистичная лампочка внутри  
**Минусы**: Нужно позиционировать для каждой модели, требует shadowMap

```javascript
const interiorLight = new THREE.PointLight(0xffffff, 1.0, 3000);
interiorLight.position.set(0, 1500, 0);  // Внутри шкафа
this.scene.add(interiorLight);
```

### Вариант 2: Увеличить Ambient до 1.2
**Плюсы**: Самое простое  
**Минусы**: Убивает контраст, сцена становится "плоской"

```javascript
const ambientLight = new THREE.AmbientLight(0xffffff, 1.2);
```

### Вариант 3: RectAreaLight (плоский источник)
**Плюсы**: Реалистичное панельное освещение  
**Минусы**: Не поддерживает тени, высокая нагрузка

```javascript
const rectLight = new THREE.RectAreaLight(0xffffff, 2.0, 1000, 500);
rectLight.position.set(0, 1500, 0);
```

**Почему выбран HemisphereLight**:
✅ Низкая нагрузка (нет shadow maps)  
✅ Естественный вид (градиент яркости)  
✅ Не требует позиционирования для каждой модели  
✅ Работает с любой геометрией  

---

## Итоговая конфигурация освещения

**Все источники света** (после изменений):

```javascript
// 1. Key Light (основной, сверху-справа)
DirectionalLight(0xffffff, 0.8) + castShadow

// 2. Fill Light (заполняющий, слева)
DirectionalLight(0xffffff, 0.6)

// 3. Rim Light (контровый, сзади)
DirectionalLight(0xffffff, 0.2)

// 4. Ambient Light (глобальный)
AmbientLight(0xffffff, 0.8)  ← УВЕЛИЧЕН с 0.6

// 5. Hemisphere Light (небо + земля)
HemisphereLight(0xffffff, 0x444444, 0.4)  ← НОВЫЙ

// Итого: 5 источников света
```

**Суммарная интенсивность**:
- Ambient: 0.8
- Hemisphere: 0.4
- Key: 0.8
- Fill: 0.6
- Rim: 0.2
**Итого внутри шкафа**: ~1.2 (Ambient + Hemisphere)

---

## Отладка

### Проблема: "Внутренности всё равно тёмные"

**Чек-лист**:
1. ✅ `hemisphereLight` добавлен в сцену (console: `💡 Hemisphere Light добавлен`)
2. ✅ `ambientIntensity = 0.8` (было 0.6)
3. ✅ GUI-слайдер Interior Light ≥ 0.4
4. ✅ Модель имеет внутренние поверхности (не hollow geometry)

**Проверьте в DevTools Console**:
```javascript
// Откройте Console и выполните:
const hemi = app.sceneManager.hemisphereLight;
console.log('Hemisphere intensity:', hemi.intensity);  // Должно быть 0.4
console.log('Sky color:', hemi.color.getHexString());   // ffffff
console.log('Ground color:', hemi.groundColor.getHexString());  // 444444
```

### Проблема: "Внутренности слишком яркие"
→ **Решение**: GUI → Lighting → 🔦 Interior Light → установите 0.2

### Проблема: "Нет градиента яркости (верх/низ одинаковые)"
→ **Причина**: Ambient слишком сильный перебивает Hemisphere  
→ **Решение**: Уменьшите Ambient до 0.5, увеличьте Hemisphere до 0.8

---

## Полезные ссылки

**Three.js документация**:
- [HemisphereLight](https://threejs.org/docs/#api/en/lights/HemisphereLight)
- [AmbientLight](https://threejs.org/docs/#api/en/lights/AmbientLight)
- [Lighting примеры](https://threejs.org/examples/#webgl_lights_hemisphere)

**Теория освещения**:
- [3-point lighting](https://en.wikipedia.org/wiki/Three-point_lighting) — классическая схема (Key, Fill, Rim)
- [Hemisphere lighting](https://docs.unity3d.com/Manual/lighting-ambient-light.html) — Unity документация

---

✅ **Готово! Обновите страницу (Ctrl+Shift+R) и проверьте освещение внутренностей.**
