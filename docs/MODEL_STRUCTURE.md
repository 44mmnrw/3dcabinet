# Структура 3D-модели шкафа tsh_700_500_240.glb

## 📦 Общая информация

- **Файл**: `public/assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.glb`
- **Размер**: 716.18 KB
- **Generator**: Khronos glTF Blender I/O v4.5.48
- **Формат**: glTF 2.0
- **Единицы измерения**: Метры

## 🌲 Иерархия объектов

```
tsh_700_500_250 (корневой узел)
├── BODY              - Корпус шкафа
├── DIN_RAIL_1        - DIN-рейка #1
├── DIN_RAIL_2        - DIN-рейка #2
├── DIN_RAIL_3        - DIN-рейка #3
├── DOOR              - Дверца (с петлями)
├── INSULATION        - Теплоизоляция
├── INSULATION_FRAME  - Рама теплоизоляции
└── PANEL_003         - Монтажная панель (для оборудования)
```

## 📐 Позиции объектов (локальные координаты)

| Объект            | X (м)    | Y (м)   | Z (м)    | Примечание |
|-------------------|----------|---------|----------|------------|
| BODY              | -0.2337  | 0.0013  | -0.1248  | Базовая позиция |
| DIN_RAIL_1        | -0.2337  | 0.0013  | -0.1248  | = BODY |
| DIN_RAIL_2        | -0.2337  | 0.0013  | -0.1248  | = BODY |
| DIN_RAIL_3        | -0.2337  | 0.0013  | -0.1248  | = BODY |
| DOOR              | -0.2427  | 0.3624  | 0.1171   | **Смещён!** (петли) |
| INSULATION        | -0.2337  | 0.0013  | -0.1258  | Почти = BODY |
| INSULATION_FRAME  | -0.2337  | 0.0013  | -0.1248  | = BODY |
| PANEL_003         | -0.2337  | 0.0013  | -0.1248  | = BODY |

### ⚠️ Важно: DOOR имеет отличную позицию!

**DOOR** смещён на `(-0.009, 0.361, 0.242)` относительно остальных объектов.
Это позиция **петель двери** — точка вращения при анимации открывания.

## 🔧 Использование в коде

### Поиск объектов по имени

```javascript
// Панель для монтажа оборудования
const panel = model.getObjectByName('PANEL_003') || 
              model.getObjectByName('PANEL.003') ||  // fallback
              model.getObjectByName('PANEL003');     // fallback

// Корпус шкафа
const body = model.getObjectByName('BODY');

// Дверца
const door = model.getObjectByName('DOOR');

// DIN-рейки
const dinRails = [
    model.getObjectByName('DIN_RAIL_1'),
    model.getObjectByName('DIN_RAIL_2'),
    model.getObjectByName('DIN_RAIL_3')
].filter(Boolean);
```

### Автоматический поиск (текущая реализация)

**CabinetModel.js** использует:
- `findDoor()` — ищет по паттерну 'door', 'Door', 'DOOR' и т.д.
- `findDinRails()` — ищет по паттерну 'din', 'rail', 'DIN', 'RAIL'

## 🎨 Материалы и текстуры

- **Материалов**: 8
- **Текстур**: 15
- Используются PBR-материалы из KeyShot/Blender

## 📝 Примечания для разработчиков

### Проблема смещения DOOR

DOOR имеет локальную позицию, отличную от остальных объектов, что вызывает проблемы при клонировании для outline-эффекта:

**Проблема**: При `mesh.clone()` копируется только геометрия меша, но не world transform родительских объектов.

**Решение**: В `ShaderUtils.js` используется копирование world transform:
```javascript
mesh.updateMatrixWorld(true);
outline.position.copy(mesh.getWorldPosition(new THREE.Vector3()));
outline.quaternion.copy(mesh.getWorldQuaternion(new THREE.Quaternion()));
outline.scale.copy(mesh.getWorldScale(new THREE.Vector3()));
```

### Pivot Offset

При загрузке модели в `CabinetModel.js` применяется `pivotOffset`:
```javascript
this.pivotOffset.set(-center.x, -box.min.y, -center.z);
this.model.position.copy(this.pivotOffset);
```

Это центрирует шкаф по XZ и ставит его на пол (Y=0).

**Примерный pivotOffset**: `(0.2337, -0.0013, 0.1248)`

## 🔄 История изменений

- **12.11.2025**: Обновлены названия объектов с `PANEL003` → `PANEL_003`, `DIN_RAIL_40001` → `DIN_RAIL_1`
- **12.11.2025**: Документирована структура модели и позиции объектов
- **12.11.2025**: Исправлена проблема смещения outline для DOOR

## 📚 Связанные файлы

- `public/js/modules/CabinetModel.js` - Загрузка и управление моделью
- `public/js/modules/ShaderUtils.js` - Создание outline-эффекта
- `public/js/pages/configurator.js` - Работа с режимом сборки
