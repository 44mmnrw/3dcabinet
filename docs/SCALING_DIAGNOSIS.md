# 🔍 Диагностика масштабирования моделей

## Проблема
Выключатель выглядит гигантским, шкаф маленьким, хотя в Blender их размеры соответствуют друг другу.

## Причины

### 1. Разные единицы измерения в GLB
**Blender позволяет экспортировать в разных единицах:**
- Метры (m) — множитель 1.0
- Сантиметры (cm) — множитель 0.01
- Миллиметры (mm) — множитель 0.001

**Проблема:** Если шкаф экспортирован в мм, а выключатель в метрах — получается разница в 1000x!

### 2. Разные методы масштабирования в коде

**Шкаф** (`CabinetModel.js`, строка 98):
```javascript
scaleFactor = expectedDiagonal / initialDiagonal;
```
Масштабируется по **диагонали** (√(x²+y²+z²))

**Оборудование** (`configurator.js`, строка 310):
```javascript
const scaleFactor = realWidth / originalSize.x;
```
Масштабируется по **ширине X**

### 3. Вложенное масштабирование
Оборудование добавляется **ВНУТРЬ** масштабированного шкафа через `cabinet.addEquipment()`, что применяет масштаб шкафа к оборудованию!

```javascript
// В CabinetModel.js, метод addEquipment():
this.model.add(equipmentModel); // ← Наследует scale от this.model!
```

## Решение

### Шаг 1: Проверить логи (СДЕЛАНО ✅)

Добавлено подробное логирование:
- Исходные размеры из GLB
- Вычисленный scaleFactor
- Размеры после масштабирования
- Единицы измерения (1 единица GLB = X мм)

### Шаг 2: Откройте Console и найдите логи

**Для шкафа:**
```
📦 Исходные размеры ШКАФА (из GLB):
  └─ Ширина (X): XXX.XX единиц
  └─ Высота (Y): XXX.XX единиц
  └─ Глубина (Z): XXX.XX единиц

🎯 Целевые размеры (из config):
  width: 700, height: 500, depth: 240

🔢 Вычисленный scaleFactor: X.XXXXXXx
💡 В GLB шкафа: 1 единица = X.XX мм

✅ Размеры ШКАФА после масштабирования:
  └─ Ширина: XXX.XX мм (ожидалось 700 мм)
```

**Для оборудования:**
```
📦 Исходные размеры оборудования (из GLB):
  └─ X (ширина): XX.XX единиц
  └─ Y (высота): XX.XX единиц
  └─ Z (глубина): XX.XX единиц

🎯 Целевые размеры (реальные):
  width: 18, height: 90, depth: 70

🔢 Вычисленный scaleFactor: X.XXXXXXx
💡 Это значит, что в GLB 1 единица = XX.XX мм

✅ Размеры после масштабирования:
  └─ Ширина: XX.XX мм (ожидалось 18 мм)
```

### Шаг 3: Анализ логов

#### Сценарий A: Разные единицы в GLB

**Пример:**
```
Шкаф: 1 единица = 0.70 мм    (экспортирован в метрах, 700 единиц = 700м = 700000мм)
Оборудование: 1 единица = 1.00 мм  (экспортирован в мм, 18 единиц = 18мм)
```

**Решение:** Пересоздать GLB с одинаковыми единицами измерения в Blender

#### Сценарий B: Одинаковые единицы, но неправильный масштаб

**Пример:**
```
Шкаф: 1 единица = 1.00 мм
Оборудование: 1 единица = 1.00 мм
Но в коде scaleFactor = 0.001 (вместо 1.0)
```

**Решение:** Исправить код масштабирования

#### Сценарий C: Вложенное масштабирование

**Пример:**
```
Шкаф: scale = 10x
Оборудование добавлено внутрь шкафа → наследует scale 10x
Итоговый scale оборудования: 10 * 0.5 = 5x (вместо 0.5x)
```

**Решение:** Добавлять оборудование не внутрь `cabinet.model`, а в корень сцены

---

## Варианты исправления

### Вариант 1: Унифицировать единицы в Blender (РЕКОМЕНДУЕТСЯ)

**Шаги:**
1. Откройте оба GLB в Blender
2. **Scene Properties → Units → Unit Scale**
3. Установите одинаковые единицы (например, Millimeters)
4. Убедитесь, что размеры совпадают:
   - Шкаф: 700×500×240 мм
   - Выключатель: 18×90×70 мм
5. Экспортируйте оба GLB с одинаковыми настройками:
   ```
   File → Export → glTF 2.0
   Transform → +Y Up
   Include → Apply Modifiers
   ```

**Преимущества:**
- ✅ Решает проблему раз и навсегда
- ✅ Не требует изменений в коде
- ✅ Все будущие модели будут работать корректно

### Вариант 2: Исправить код масштабирования

Если пересоздать GLB нельзя, измените код:

**Файл:** `public/js/pages/configurator.js`, строка ~310

```javascript
// БЫЛО (масштабирование по ширине):
const scaleFactor = realWidth / originalSize.x;

// СТАЛО (учёт единиц измерения):
const equipmentUnitsPerMM = 1.0; // ← Из логов! (1 единица = X мм)
const cabinetUnitsPerMM = 0.7;   // ← Из логов шкафа!

const scaleFactor = (realWidth * equipmentUnitsPerMM) / (originalSize.x * cabinetUnitsPerMM);
```

**НО!** Это костыль, лучше исправить модели.

### Вариант 3: Добавить оборудование в корень сцены

Избежать наследования масштаба шкафа:

**Файл:** `public/js/modules/CabinetModel.js`, метод `addEquipment()`

```javascript
// БЫЛО:
this.model.add(equipmentModel); // Наследует scale

// СТАЛО:
this.model.parent.add(equipmentModel); // Добавить в сцену, не в шкаф
// Затем позиционировать относительно рейки:
const railWorldPos = new THREE.Vector3();
rail.getWorldPosition(railWorldPos);
equipmentModel.position.copy(railWorldPos);
```

**Проблема:** Усложняет код, нарушает иерархию

---

## Чеклист диагностики

Откройте http://localhost:8000/app и проверьте Console:

- [ ] Нашли логи "📦 Исходные размеры ШКАФА"
- [ ] Нашли логи "📦 Исходные размеры оборудования"
- [ ] Записали значение "1 единица = X.XX мм" для шкафа: _______
- [ ] Записали значение "1 единица = X.XX мм" для оборудования: _______
- [ ] Сравнили значения — они одинаковые? ДА / НЕТ
- [ ] Размеры после масштабирования близки к ожидаемым? ДА / НЕТ

---

## Следующий шаг

**СЕЙЧАС:** Обновите страницу http://localhost:8000/app и скопируйте логи из Console.

**ЗАТЕМ:** В зависимости от логов:
1. Если единицы разные → пересоздать GLB в Blender (Вариант 1)
2. Если единицы одинаковые, но масштаб неправильный → исправить код (Вариант 2)
3. Если есть вложенное масштабирование → изменить addEquipment (Вариант 3)

---

## Файлы с изменениями

- ✅ `public/js/modules/CabinetModel.js` — добавлено подробное логирование масштабирования шкафа
- ✅ `public/js/pages/configurator.js` — добавлено подробное логирование масштабирования оборудования

**Изменения:** Только логи для диагностики, логика масштабирования НЕ изменена!
