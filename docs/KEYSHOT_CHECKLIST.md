# 🔍 Чеклист проверки текстур KeyShot

Используйте этот файл для быстрой проверки статуса текстур.

## ✅ Файлы на месте

```powershell
# Проверить наличие файлов
dir public\assets\textures\metal\keyshot\*.jpg
dir public\assets\textures\metal\keyshot\*.png

# Проверить размеры (не должны быть 0 KB!)
Get-ChildItem public\assets\textures\metal\keyshot\ | Select-Object Name, Length
```

**Ожидаемый результат:**
```
Name                      Length
----                      ------
brushed_albedo.jpg        800000-3000000  (зависит от разрешения)
brushed_normal.png        2000000-8000000 (PNG больше JPG)
brushed_roughness.jpg     500000-2000000
brushed_ao.jpg            500000-2000000  (если есть)
brushed_metalness.jpg     500000-2000000  (если есть)
```

---

## 🌐 Сервер запущен

```powershell
# Запустить Laravel
php artisan serve

# Должно быть:
# Laravel development server started: http://127.0.0.1:8000
```

Откройте: **http://localhost:8000/app**

---

## 🖥️ DevTools Console (F12)

### ✅ Успешная загрузка
Должны быть логи:
```
🔄 CabinetModel.load() начат для: /assets/models/...
✅ GLTF загружен успешно: /assets/models/...
🖼️ Применение текстур...
✅ Текстуры применены к body
✅ Текстуры применены к door
✅ Текстуры применены к panel
✅✅✅ CabinetModel.load() ЗАВЕРШЁН УСПЕШНО ✅✅✅
```

### ❌ Ошибки
Если есть ошибки 404:
```
GET http://localhost:8000/assets/textures/metal/keyshot/brushed_albedo.jpg 404 (Not Found)
```

**Решение:**
1. Проверьте, что файл существует: `dir public\assets\textures\metal\keyshot\brushed_albedo.jpg`
2. Имя файла точно совпадает (регистр важен!)
3. Перезапустите сервер: `php artisan serve`

---

## 🌐 Network Tab (DevTools → Network)

### Фильтр: `brushed`

Должны загружаться файлы:
- ✅ `brushed_albedo.jpg` — **Status: 200**, Size: ~800KB-3MB
- ✅ `brushed_normal.png` — **Status: 200**, Size: ~2MB-8MB
- ✅ `brushed_roughness.jpg` — **Status: 200**, Size: ~500KB-2MB
- ⚪ `brushed_ao.jpg` — опционально
- ⚪ `brushed_metalness.jpg` — опционально

### ❌ Если Status: 404
```powershell
# Проверить структуру папок
tree /F public\assets\textures\metal\keyshot\
```

---

## 🎨 Визуальная проверка

### Что должно быть видно:
- ✅ Модель шкафа отображается
- ✅ Металлические части имеют текстуру (не плоский цвет)
- ✅ Видна шероховатость поверхности
- ✅ Блики света реалистичные
- ✅ Углубления затенены (если есть AO)

### ❌ Если модель однотонная:
1. **Console → ищите ошибки** загрузки текстур
2. **Network → проверьте** что текстуры загрузились (200 OK)
3. **Файлы:** убедитесь что экспортировали из KeyShot

---

## 🧪 Тестовые команды

### Проверить все файлы проекта
```powershell
# Структура папок
tree /F public\assets\textures\

# Размер текстур
Get-ChildItem public\assets\textures\ -Recurse -File | Select-Object FullName, Length | Format-Table -AutoSize

# Права доступа (если на Linux/Mac)
ls -lh public/assets/textures/metal/keyshot/
```

### Очистить кеш Laravel
```powershell
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

### Проверить роуты
```powershell
php artisan route:list | Select-String "app"
```

---

## 📋 Итоговый чеклист

Перед тем как открыть issue, проверьте:

- [ ] Файлы текстур существуют в `public/assets/textures/metal/keyshot/`
- [ ] Имена файлов правильные (с суффиксами `_albedo`, `_normal`, `_roughness`)
- [ ] Normal map в формате PNG (не JPG!)
- [ ] Размеры файлов разумные (не 0 KB)
- [ ] Сервер запущен: `php artisan serve`
- [ ] Страница открывается: http://localhost:8000/app
- [ ] Console без ошибок 404
- [ ] Network показывает Status: 200 для всех текстур
- [ ] Модель видна в 3D-сцене

---

## 💡 Быстрая диагностика

```powershell
# 1. Проверить файлы (1 команда)
Get-ChildItem public\assets\textures\metal\keyshot\ | Select-Object Name, Length | Format-Table

# 2. Запустить сервер (если не запущен)
php artisan serve

# 3. Открыть в браузере
start http://localhost:8000/app

# 4. Открыть Console (F12) и проверить логи
```

---

## 🆘 Поддержка

Если что-то не работает:

1. **Подробная документация:** `KEYSHOT_TEXTURES_GUIDE.md`
2. **Быстрый старт:** `KEYSHOT_QUICKSTART.md`
3. **Техническая документация:** `TEXTURES_GUIDE.md`

📧 Или создайте issue с:
- Скриншот Console (F12)
- Скриншот Network tab
- Вывод команды: `dir public\assets\textures\metal\keyshot\`
