# 🚀 Быстрый старт: Текстуры из KeyShot

## 📥 Шаг 1: Экспорт из KeyShot

### Открыть Material Graph
1. В KeyShot: **Правый клик на материале** → **Edit in Material Graph**
2. Найдите ноды с текстурами

### Экспортировать текстуры
Для каждой ноды (Color, Roughness, Bump):
```
Правый клик → Export Texture
Разрешение: 2048×2048
Формат: JPG для color/roughness, PNG для normal
```

**Что экспортировать:**
- ✅ **Color** → сохранить как `brushed_albedo.jpg`
- ✅ **Bump/Normal** → сохранить как `brushed_normal.png` (16-bit PNG!)
- ✅ **Roughness** → сохранить как `brushed_roughness.jpg`
- ⚪ **AO** (если есть) → `brushed_ao.jpg`
- ⚪ **Metallic** (если есть) → `brushed_metalness.jpg`

---

## 📁 Шаг 2: Разместить файлы

Скопируйте экспортированные файлы в:
```
public/assets/textures/metal/keyshot/
├── brushed_albedo.jpg       ← ОБЯЗАТЕЛЬНО
├── brushed_normal.png       ← ОБЯЗАТЕЛЬНО (16-bit PNG!)
├── brushed_roughness.jpg    ← ОБЯЗАТЕЛЬНО
├── brushed_ao.jpg           ← опционально
└── brushed_metalness.jpg    ← опционально
```

⚠️ **ВАЖНО:** Имена файлов должны точно совпадать с суффиксами!

---

## ✅ Шаг 3: Проверка

### Запустить приложение
```powershell
php artisan serve
```

Откройте: http://localhost:8000/app

### Что проверить
1. **Console (F12):**
   - Не должно быть ошибок 404
   - Должны быть логи: `✅ Текстуры применены к [mesh_name]`

2. **3D-сцена:**
   - Модель должна быть видна
   - Текстуры должны отображаться на металлических частях

3. **Network (DevTools → Network):**
   - Текстуры загружаются (200 OK)
   - Размеры файлов разумные (не 0 KB)

---

## 🐛 Troubleshooting

### ❌ Текстуры не загружаются (404)
**Решение:**
```powershell
# Проверить, что файлы на месте
dir public\assets\textures\metal\keyshot\

# Должно быть минимум 3 файла:
# brushed_albedo.jpg
# brushed_normal.png
# brushed_roughness.jpg
```

### ❌ Нормали выглядят неправильно
**Решение:**
- Экспортируйте Normal в формате **PNG 16-bit** (не JPG!)
- В KeyShot: Material Graph → Normal → Format → **Tangent Space**

### ❌ Материал слишком блестящий
**Решение:**
- Roughness map слишком тёмная
- В Photoshop: увеличьте яркость Roughness текстуры
- Или в коде: измените `metalness: 0.3` → `0.5` в `CabinetModel.js:283`

### ❌ Модель не видна совсем
**Решение:**
1. Console → проверьте ошибки загрузки GLB
2. Проверьте путь к модели: `/assets/models/thermocabinets/tsh_700_500_240/tsh_700_500_240.glb`
3. Файл `.glb` должен существовать в `public/assets/models/...`

---

## 🎨 Изменить текстуры для других частей

Откройте `public/js/pages/configurator.js`, строка ~207:

```javascript
const textureScheme = {
    body: '/assets/textures/metal/keyshot/brushed',
    door: '/assets/textures/metal/keyshot/brushed',
    panel: '/assets/textures/metal/keyshot/painted',  // ← Другая текстура
    dinRail: '/assets/textures/metal/keyshot/galvanized'
};
```

Для каждой новой текстуры нужны файлы:
```
public/assets/textures/metal/keyshot/
├── painted_albedo.jpg
├── painted_normal.png
└── painted_roughness.jpg
```

---

## 📚 Подробная документация

- **`KEYSHOT_TEXTURES_GUIDE.md`** — полное руководство по экспорту
- **`TEXTURES_GUIDE.md`** — техническая документация PBR-системы
- **`COLOR_QUICK_GUIDE.md`** — как изменить цвета без текстур

---

## ✨ Готово!

После добавления текстур шкаф будет выглядеть реалистично с:
- ✅ Металлическим блеском (metalness)
- ✅ Шероховатостью поверхности (roughness)
- ✅ Объёмными деталями (normal map)
- ✅ Затенением в углублениях (ambient occlusion)

🎯 Текстуры применяются автоматически при загрузке модели!
