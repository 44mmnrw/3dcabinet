# Тестовые текстуры

Для тестирования системы текстур создайте простую тестовую текстуру:

## Вариант 1: Создать простую JPG-текстуру

Создайте файл `public/assets/textures/metal/brushed/brushed_albedo.jpg` размером 512×512 пикселей с любым металлическим паттерном.

## Вариант 2: Скачать готовую

Посетите https://polyhaven.com/textures и скачайте любую металлическую текстуру:
1. Выберите "Metal" категорию
2. Скачайте JPG версию (1K достаточно)
3. Переименуйте в `brushed_albedo.jpg`
4. Поместите в `public/assets/textures/metal/brushed/`

## Структура для теста

```
public/assets/textures/
└── metal/
    └── brushed/
        ├── brushed_albedo.jpg       (обязательно!)
        ├── brushed_normal.jpg       (опционально)
        ├── brushed_roughness.jpg    (опционально)
        └── brushed_ao.jpg           (опционально)
```

## Применение тестовой текстуры

```javascript
// В configurator.js в loadTestCabinet()
const cabinet = new CabinetModel(modelPath, {
    type: 'floor',
    width: 700,
    height: 500,
    depth: 240,
    name: 'TSH 700×500×240',
    color: this.cabinetColorScheme.default,
    colorScheme: this.cabinetColorScheme,
    // 🎨 ДОБАВЬТЕ ЭТУ СТРОКУ:
    textureScheme: {
        body: '/assets/textures/metal/brushed'
    }
});
```

Система автоматически загрузит все доступные карты текстур!
