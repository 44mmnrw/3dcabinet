# Руководство по созданию GLB-моделей шкафов для 3DCabinet

## 📋 Оглавление
1. [Требования к моделям](#требования-к-моделям)
2. [Структура модели](#структура-модели)
3. [Настройка осей и pivot point](#настройка-осей-и-pivot-point)
4. [Материалы и текстуры](#материалы-и-текстуры)
5. [Экспорт в GLB](#экспорт-в-glb)
6. [Проверка модели](#проверка-модели)
7. [Типичные ошибки](#типичные-ошибки)

---

## Требования к моделям

### Общие требования
- **Формат**: GLB (бинарный GLTF 2.0)
- **Единицы измерения**: Миллиметры (мм)
- **Система координат**: Y-up (Y вверх, Z вперёд, X вправо)
- **Масштаб**: 1:1 (реальные размеры)
- **Полигональность**: Low-poly для веб-оптимизации (до 50k треугольников на шкаф)

### Размеры шкафов
Стандартные размеры в мм:
- **Ширина**: 500-1000 мм
- **Высота**: 400-2000 мм
- **Глубина**: 200-800 мм

**Пример**: `tsh_700_500_240.glb` = Ширина 700 × Высота 500 × Глубина 240 мм

---

## Структура модели

### Обязательные части (Naming Convention)

#### 1. **BODY** (корпус шкафа)
- **Имя объекта**: `BODY` (регистр важен!)
- **Описание**: Основной корпус шкафа (стенки, пол, потолок)
- **Материал**: Металл/пластик, цвет задаётся программно
- **Примечание**: НЕ включайте сюда дверь и внутренние компоненты

#### 2. **DOOR** (дверь шкафа)
- **Имя объекта**: `DOOR` (регистр важен!)
- **Описание**: Дверца шкафа, должна быть отдельным объектом
- **Материал**: Металл/стекло
- **⚠️ КРИТИЧНО**: См. раздел [Настройка pivot point для двери](#настройка-pivot-point-для-двери)

#### 3. **INSULATION** (изоляция) — опционально
- **Имя объекта**: `INSULATION`
- **Описание**: Термоизоляционный слой (для термошкафов)
- **Материал**: Белый/серый пенополистирол

#### 4. **INSULATION_FRAME** (рамка изоляции) — опционально
- **Имя объекта**: `INSULATION_FRAME`
- **Описание**: Обрамление изоляции
- **Материал**: Металл/пластик

#### 5. **PANEL** (панель монтажная) — опционально
- **Имя объекта**: `PANEL`, `PANEL.001`, `PANEL.002`, и т.д.
- **Описание**: Монтажная панель для оборудования
- **Материал**: Металл

#### 6. **DIN_RAIL** (DIN-рейки) — опционально
- **Имя объектов**: `DIN_RAIL`, `DIN_RAIL.001`, `DIN_RAIL.002`, и т.д.
- **Описание**: Стандартные DIN-рейки 35мм для монтажа оборудования
- **Материал**: Металл
- **Примечание**: Поиск в коде ведётся по ключевым словам "din" и "rail"

### Пример иерархии в Blender

```
Scene
└── MKP92-N-705024 (root Empty/Object3D)
    ├── BODY (Mesh)
    ├── DOOR (Mesh)
    ├── INSULATION (Mesh)
    ├── INSULATION_FRAME (Mesh)
    ├── PANEL.001 (Mesh)
    ├── PANEL.002 (Mesh)
    ├── PANEL.003 (Mesh)
    ├── DIN_RAIL_40 (Object3D или Empty)
    │   ├── DIN_RAIL_40.001 (Mesh)
    │   ├── DIN_RAIL_40.002 (Mesh)
    │   └── DIN_RAIL_40.003 (Mesh)
```

---

## Настройка осей и pivot point

### Ориентация модели

#### Система координат Three.js (используется в проекте):
- **Ось X** (красная): Горизонтальная, слева (-) → справа (+)
- **Ось Y** (зелёная): Вертикальная, вниз (-) → вверх (+)
- **Ось Z** (синяя): Глубина, назад (-) → вперёд (+)

#### Правильная ориентация шкафа:
```
      Y (вверх)
      ↑
      |
      |______ X (вправо)
     /
    /
   Z (вперёд)
```

- **Передняя стенка** (с дверью): смотрит в направлении **+Z**
- **Дверь**: открывается вокруг **вертикальной оси Y**
- **Пол шкафа**: должен быть на уровне **Y = 0** (или ниже)

### Настройка Origin для всей модели

1. Выберите **весь шкаф** (все объекты)
2. `Object` → `Set Origin` → `Origin to Geometry`
3. В режиме **Edit Mode** (Tab) переместите геометрию так, чтобы:
   - **Центр по X**: геометрический центр модели
   - **Центр по Z**: геометрический центр модели
   - **Низ по Y**: `Y = 0` (пол шкафа на уровне земли)

**Результат**: Origin модели должен быть в центре основания шкафа (X=0, Y=0, Z=0).

### Настройка pivot point для двери

#### ⚠️ КРИТИЧЕСКИ ВАЖНО! Без этого дверь будет вращаться вокруг центра, а не петель!

1. **Выберите объект DOOR** в Blender
2. Перейдите в **Edit Mode** (Tab)
3. Выделите **ребро или вершины**, где должны быть **петли** (обычно левый или правый край двери)
4. Нажмите `Shift + S` → **Cursor to Selected** (курсор переместится к петлям)
5. Вернитесь в **Object Mode** (Tab)
6. `Object` → `Set Origin` → **Origin to 3D Cursor**
7. **Проверьте**: Origin двери теперь должен быть **на краю с петлями**, а не в центре!

#### Визуальная проверка Origin двери:
В Blender включите отображение осей объекта:
- `Viewport Overlays` → `Origins` → `All`
- Origin двери (оранжевая точка) должен быть **на краю**, где петли

#### Позиция петель:
- **Левые петли**: Origin на левом краю двери (X = min)
- **Правые петли**: Origin на правом краю двери (X = max)

### Сброс rotation и scale

**Перед экспортом** убедитесь, что все объекты имеют нулевые rotation и единичный scale:

1. Выберите **все объекты** (A)
2. `Object` → `Apply` → `All Transforms` (или по отдельности):
   - `Rotation` (Ctrl+A → Rotation)
   - `Scale` (Ctrl+A → Scale)
3. **Проверьте** в панели Transform:
   - Rotation: `X=0°, Y=0°, Z=0°`
   - Scale: `X=1, Y=1, Z=1`

#### ⚠️ Особенно важно для двери!
После применения трансформаций **дверь должна иметь**:
- `rotation.x = 0`
- `rotation.y = 0`
- `rotation.z = 0`

Если дверь изначально повёрнута (например, Y=-90°), это вызовет проблемы с вращением в Three.js!

---

## Материалы и текстуры

### Рекомендуемые материалы

#### 1. Металл (корпус, дверь)
- **Base Color**: Светло-серый `#CCCCCC` (программно изменяется)
- **Metallic**: 0.8-1.0
- **Roughness**: 0.3-0.5
- **Normal Map**: Опционально (для детализации)

#### 2. Пластик (изоляция, рамки)
- **Base Color**: Белый `#FFFFFF` или серый `#DDDDDD`
- **Metallic**: 0.0
- **Roughness**: 0.4-0.6

#### 3. Стекло (дверь с окном)
- **Base Color**: Светло-синий `#E0F0FF`
- **Metallic**: 0.0
- **Roughness**: 0.05
- **Transmission**: 0.9 (прозрачность)
- **IOR**: 1.5

### Текстуры (опционально)

- **Формат**: PNG или JPEG (PNG для прозрачности)
- **Разрешение**: 1024×1024 или 2048×2048 (для веб-оптимизации)
- **Типы**:
  - Base Color (Albedo)
  - Normal Map
  - Metallic/Roughness (упакованные в один файл)
- **Встраивание**: Текстуры должны быть **embedded** в GLB (настройка экспорта)

### UV-развёртка

- Используйте **Smart UV Project** или ручную развёртку
- Проверьте, что все грани имеют UV-координаты
- Избегайте перекрытия UV-островов (для уникальных текстур)

---

## Экспорт в GLB

### Настройки экспорта в Blender

1. `File` → `Export` → `glTF 2.0 (.glb/.gltf)`

2. **Вкладка "Include"**:
   - ✅ **Selected Objects** (если нужны только выбранные) или
   - ✅ **Visible Objects** (экспортировать всё видимое)
   - ✅ **Custom Properties** (опционально)
   - ✅ **Cameras** — ❌ (не нужны)
   - ✅ **Punctual Lights** — ❌ (не нужны)

3. **Вкладка "Transform"**:
   - **Y Up**: ✅ (обязательно!)
   - **+Y Up**: ✅
   - **Forward**: -Z Forward (по умолчанию)

4. **Вкладка "Geometry"**:
   - ✅ **Apply Modifiers** (применить модификаторы)
   - ✅ **UVs** (экспортировать UV)
   - ✅ **Normals** (экспортировать нормали)
   - ✅ **Tangents** (для Normal Map)
   - ✅ **Vertex Colors** (если используются)
   - **Compression**: Draco (опционально, для уменьшения размера файла)

5. **Вкладка "Materials"**:
   - **Materials**: ✅ Export
   - **Images**: **Automatic** (встроить текстуры в GLB)

6. **Вкладка "Animation"**:
   - ❌ (анимация не нужна, вращение двери реализовано в коде)

7. Нажмите **Export glTF 2.0**

### Соглашение об именовании файлов

Формат: `{тип}_{ширина}_{высота}_{глубина}.glb`

**Примеры**:
- `tsh_700_500_240.glb` — Термошкаф 700×500×240 мм
- `mkp_600_800_300.glb` — Монтажный шкаф 600×800×300 мм
- `rack_800_2000_1000.glb` — Серверный шкаф 800×2000×1000 мм

**Типы шкафов**:
- `tsh` — Термошкаф (ThermoСabinet)
- `mkp` — Монтажный шкаф (Mounting Cabinet)
- `rack` — Серверный шкаф (Rack)
- `wall` — Навесной шкаф (Wall-mount)

---

## Проверка модели

### Чек-лист перед загрузкой в проект

#### 1. Структура и именование
- [ ] Объект `BODY` существует
- [ ] Объект `DOOR` существует (если есть дверь)
- [ ] Все имена объектов в ВЕРХНЕМ РЕГИСТРЕ
- [ ] DIN-рейки содержат "DIN" или "RAIL" в имени

#### 2. Трансформации
- [ ] Rotation всех объектов: `X=0, Y=0, Z=0`
- [ ] Scale всех объектов: `X=1, Y=1, Z=1`
- [ ] Origin двери на краю с петлями (НЕ в центре!)
- [ ] Origin всей модели в центре основания (X=0, Y=0, Z=0)

#### 3. Размеры
- [ ] Модель в правильных единицах (мм)
- [ ] Размеры соответствуют реальным (проверьте bounding box)
- [ ] Пол шкафа на уровне Y=0 или ниже

#### 4. Материалы
- [ ] Все объекты имеют материалы
- [ ] Текстуры embedded в GLB (если используются)
- [ ] Нет ошибок с материалами (розовый/фиолетовый цвет = ошибка)

#### 5. Геометрия
- [ ] Нет перевёрнутых нормалей (чёрные грани)
- [ ] Нет дублированных вершин (Mesh → Clean Up → Merge by Distance)
- [ ] Полигональность оптимизирована (< 50k треугольников)

### Проверка в Three.js Editor

1. Откройте [Three.js Editor](https://threejs.org/editor/)
2. Перетащите GLB-файл в окно
3. Проверьте:
   - Модель отображается корректно
   - Размеры (посмотрите в панели Scene)
   - Ориентация осей (добавьте AxesHelper)
   - Pivot point двери (выберите DOOR → посмотрите на origin)

### Проверка в проекте 3DCabinet

1. Поместите GLB в `public/assets/models/thermocabinets/{folder}/`
2. Обновите путь в `configurator.js`:
   ```javascript
   const testCabinet = await cabinetManager.addCabinet({
       modelPath: '/assets/models/thermocabinets/your_folder/your_model.glb',
       type: 'floor',
       width: 700,  // мм
       height: 500, // мм
       depth: 240   // мм
   });
   ```
3. Откройте консоль браузера (F12) и проверьте логи:
   ```
   ✓ Найдена дверца: "DOOR" (тип: Mesh)
   Исходный поворот дверцы: {x: 0, y: 0, z: 0, order: 'XYZ'}
   X: 0.0000 (0.0°)  ← Должен быть 0!
   Y: 0.0000 (0.0°)  ← Должен быть 0!
   Z: 0.0000 (0.0°)  ← Должен быть 0!
   ```
4. Попробуйте открыть дверь (двойной клик или клавиша `O`)
5. Проверьте, что дверь вращается **вокруг петель**, а не центра

---

## Типичные ошибки

### ❌ Ошибка 1: Дверь вращается вокруг центра
**Причина**: Origin двери не на краю с петлями

**Решение**:
1. Blender → Выберите DOOR
2. Edit Mode → выделите край с петлями
3. Shift+S → Cursor to Selected
4. Object Mode → Object → Set Origin → Origin to 3D Cursor
5. Экспортируйте заново

### ❌ Ошибка 2: Дверь не найдена в коде
**Симптом**: В консоли `⚠ Дверца не найдена в модели`

**Причина**: Объект называется не `DOOR` или регистр неправильный

**Решение**: Переименуйте объект двери в **DOOR** (все буквы заглавные)

### ❌ Ошибка 3: Дверь имеет ненулевой rotation
**Симптом**: 
```
Исходный поворот дверцы: {x: 0, y: -1.5705, z: 0}
Y: -1.5705 (-90.0°)  ← Проблема!
```

**Причина**: Не применён rotation перед экспортом

**Решение**:
1. Blender → Выберите DOOR
2. Object → Apply → Rotation (Ctrl+A → Rotation)
3. Проверьте: Rotation должен быть `X=0, Y=0, Z=0`
4. Экспортируйте заново

### ❌ Ошибка 4: Модель слишком маленькая/большая
**Симптом**: Шкаф едва виден или огромный на сцене

**Причина**: Неправильные единицы измерения в Blender

**Решение**:
1. Blender → `Scene Properties` → `Units`
2. **Unit System**: Metric
3. **Unit Scale**: 0.001 (если работаете в мм) или 1.0 (если в м)
4. **Length**: Millimeters
5. Пересоздайте модель с правильными размерами

### ❌ Ошибка 5: Шкаф "под землёй"
**Симптом**: Нижняя часть шкафа уходит ниже плоскости пола (Y < 0)

**Причина**: Origin модели не на уровне пола

**Решение**:
1. Blender → Edit Mode (Tab)
2. Выделите ВСЮ геометрию (A)
3. Переместите (G → Y) так, чтобы низ модели был на Y=0
4. Object Mode → Apply All Transforms
5. Экспортируйте заново

### ❌ Ошибка 6: Дверь открывается не в ту сторону
**Симптом**: Дверь открывается внутрь шкафа вместо наружу (или наоборот)

**Решение**: В файле `CabinetModel.js` (строка ~517) измените знак:
```javascript
// Было:
const targetRotation = this.isDoorOpen ? baseRotation + Math.PI / 2 : baseRotation;

// Стало:
const targetRotation = this.isDoorOpen ? baseRotation - Math.PI / 2 : baseRotation;
```

### ❌ Ошибка 7: Дверь открывается вокруг неправильной оси
**Симптом**: Дверь откидывается вверх/вниз вместо поворота влево/вправо

**Решение**: В файле `CabinetModel.js` (строка ~509) измените ось:
```javascript
const ROTATION_AXIS = 'y'; // Измените на 'x', 'y' или 'z'
```
- `'y'` — вертикальная ось (для обычных дверей)
- `'x'` — горизонтальная ось (для откидных крышек)
- `'z'` — горизонтальная ось (для падающих дверей)

---

## Дополнительные рекомендации

### Оптимизация для веб

1. **Полигональность**:
   - Используйте **низкополигональные** модели (< 50k треугольников)
   - Применяйте **Decimate Modifier** для уменьшения полигонов

2. **Текстуры**:
   - Максимальное разрешение: **2048×2048** (лучше 1024×1024)
   - Используйте **JPG** для диффузных карт (меньше размер)
   - Используйте **PNG** только для прозрачности
   - Упаковывайте Metallic+Roughness в один файл (каналы R+G+B)

3. **Компрессия**:
   - При экспорте включите **Draco Compression** (уменьшает размер на 70-90%)
   - Проверьте размер GLB-файла: оптимально < 5 МБ на шкаф

4. **Освещение**:
   - НЕ экспортируйте источники света (управляется в коде)
   - Используйте **правильные материалы PBR** для реалистичного освещения

### Контроль версий

Создайте папку с версионированием:
```
models/
└── thermocabinets/
    └── tsh_700_500_240/
        ├── tsh_700_500_240.glb        ← Финальная версия
        ├── tsh_700_500_240_v1.glb     ← Старые версии (резерв)
        ├── tsh_700_500_240_v2.glb
        └── source/
            └── tsh_700_500_240.blend  ← Исходник Blender
```

---

## Полезные ссылки

- [GLTF 2.0 Specification](https://registry.khronos.org/glTF/specs/2.0/glTF-2.0.html)
- [Three.js GLTFLoader](https://threejs.org/docs/#examples/en/loaders/GLTFLoader)
- [Three.js Editor (для проверки)](https://threejs.org/editor/)
- [glTF Viewer (онлайн)](https://gltf-viewer.donmccurdy.com/)
- [Blender Export Documentation](https://docs.blender.org/manual/en/latest/addons/import_export/scene_gltf2.html)

---

## Быстрый чеклист

Перед экспортом:
- [ ] Именование: `BODY`, `DOOR`, `DIN_RAIL`, и т.д.
- [ ] Origin двери на краю (петли!)
- [ ] Rotation всех объектов: `0, 0, 0`
- [ ] Scale всех объектов: `1, 1, 1`
- [ ] Единицы: Миллиметры
- [ ] Ориентация: Y вверх, Z вперёд
- [ ] Материалы: PBR, без ошибок
- [ ] Полигоны: < 50k треугольников
- [ ] Экспорт: GLB, Y Up, Embedded textures

После импорта в проект:
- [ ] Модель видна на сцене
- [ ] Дверь открывается вокруг петель
- [ ] Размеры корректны (в консоли)
- [ ] DIN-рейки найдены (в консоли)

---

**Версия документа**: 1.0  
**Дата**: 8 ноября 2025  
**Автор**: 3DCabinet Development Team
