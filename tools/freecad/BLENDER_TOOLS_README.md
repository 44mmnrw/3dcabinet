# 🎨 Скрипты для работы в Blender

## 📋 blender_analyze_model.py

**Назначение:** Анализ модели прямо в Blender — показывает размеры, масштаб, расположение, origin, единицы измерения.

---

## 🚀 Как использовать

### Способ 1: Через Scripting workspace (рекомендуется)

1. **Откройте Blender** с вашей моделью
2. **Перейдите в Scripting** (вкладка вверху окна)
3. **Создайте новый скрипт:**
   - Text → New
   - Или нажмите `+` в верхней панели текстового редактора
4. **Скопируйте содержимое** `blender_analyze_model.py` и вставьте в окно
5. **Выделите объект** для анализа (клик по объекту в 3D Viewport)
6. **Запустите скрипт:**
   - Кнопка ▶️ "Run Script"
   - Или нажмите `Alt+P`
7. **Откройте консоль** для просмотра результатов:
   - Windows: `Window → Toggle System Console`
   - Mac/Linux: запустите Blender из терминала

### Способ 2: Через внешний файл

1. Сохраните скрипт как `blender_analyze_model.py`
2. В Blender: `Scripting → Text → Open`
3. Выберите файл `blender_analyze_model.py`
4. Выделите объект
5. Нажмите `Alt+P` или ▶️ "Run Script"

---

## 📊 Что показывает скрипт

### 1. ⚙️ Настройки сцены
- **Unit System** (Metric/Imperial)
- **Length Unit** (Millimeters/Meters/Centimeters)
- **Scale** (множитель единиц)
- ⚠️ **Предупреждения**, если единицы не миллиметры

### 2. 📐 Размеры объекта
- Dimensions в Blender units (X, Y, Z)
- **Размеры при экспорте в миллиметрах** (с учётом конверсии)
- Автоматический расчёт конечных размеров для Three.js

### 3. 📍 Позиция (Location)
- Координаты в Blender units
- Координаты при экспорте (миллиметры)
- ✅ Проверка: объект в (0, 0, 0)?

### 4. 🔄 Вращение (Rotation)
- Углы в градусах (X, Y, Z)
- ✅ Проверка: вращение = 0?
- 💡 Рекомендация: `Alt+R` для сброса

### 5. 📏 Масштаб (Scale)
- Значения по осям (X, Y, Z)
- ✅ Проверка: масштаб = 1.0?
- 💡 Рекомендация: `Ctrl+A → Scale` для применения

### 6. 🎯 Origin (точка привязки)
- Bounding Box (Min, Max, Center)
- Положение Origin в мировых координатах
- ✅ Проверка: Origin в **нижнем центре**? (идеально для шкафа!)
- 💡 Рекомендации по перемещению origin

### 7. 🔧 Модификаторы
- Список всех модификаторов
- ⚠️ Предупреждение, если нужно применить перед экспортом

### 8. 👶 Дочерние объекты (Children)
- Список всех детей
- Иконки: 🚪 дверь, 📏 рейка, 📦 прочее
- Проверка иерархии

---

## 📋 Пример вывода

```
======================================================================
📦 АНАЛИЗ МОДЕЛИ В BLENDER
======================================================================

──────────────────────────────────────────────────────────────────────
⚙️  НАСТРОЙКИ СЦЕНЫ
──────────────────────────────────────────────────────────────────────
Unit System: METRIC
Length Unit: MILLIMETERS
Scale: 1.0
✅ Единицы: МИЛЛИМЕТРЫ (правильно для экспорта!)

──────────────────────────────────────────────────────────────────────
📋 ВЫБРАНО ОБЪЕКТОВ: 1
──────────────────────────────────────────────────────────────────────

┌─ ОБЪЕКТ: CABINET_TEST
│  Тип: MESH
│
├─ 📐 РАЗМЕРЫ
│  Dimensions (Blender units):
│    X: 600.0000 мм
│    Y: 500.0000 мм
│    Z: 700.0000 мм
│  Размеры при экспорте (в миллиметрах):
│    Ширина (X):  600.00 мм
│    Высота (Z):  700.00 мм
│    Глубина (Y): 500.00 мм
│
├─ 📍 ПОЗИЦИЯ (Location)
│  Blender units:
│    X: 0.0000 мм
│    Y: 0.0000 мм
│    Z: 0.0000 мм
│  При экспорте (миллиметры):
│    X: 0.00 мм
│    Y: 0.00 мм
│    Z: 0.00 мм
│  ✅ Объект в начале координат (0, 0, 0)
│
├─ 🔄 ВРАЩЕНИЕ (Rotation)
│  Rotation (degrees):
│    X: 0.00°
│    Y: 0.00°
│    Z: 0.00°
│  ✅ Вращение = 0 (правильно!)
│
├─ 📏 МАСШТАБ (Scale)
│  Scale:
│    X: 1.0000
│    Y: 1.0000
│    Z: 1.0000
│  ✅ Масштаб = 1.0 (правильно!)
│
├─ 🎯 ORIGIN (Точка привязки)
│  Bounding Box:
│    Min: (-300.0000, -250.0000, 0.0000)
│    Max: (300.0000, 250.0000, 700.0000)
│    Center: (0.0000, 0.0000, 350.0000)
│  Origin в мировых координатах: (0.0000, 0.0000, 0.0000)
│  ✅ Origin в НИЖНЕМ ЦЕНТРЕ (идеально для шкафа!)
│
├─ 🔧 МОДИФИКАТОРЫ
│  ✅ Модификаторов нет
│
└─ 👶 ДОЧЕРНИЕ ОБЪЕКТЫ
   Всего детей: 2
     🚪 DOOR (MESH)
     📏 DIN_RAIL_1 (MESH)

======================================================================
💡 РЕКОМЕНДАЦИИ ДЛЯ ЭКСПОРТА В GLB
======================================================================
1. ✅ Единицы измерения правильные (миллиметры)

2. Перед экспортом GLB:
   • Выделите все объекты (A)
   • Object → Apply → All Transforms (Ctrl+A)
   • Проверьте, что Origin в нижнем центре

3. Настройки экспорта GLB:
   File → Export → glTF 2.0 (.glb)
   • Format: glTF Binary (.glb)
   • Transform: +Y Up
   • Geometry: Apply Modifiers ✓

4. После экспорта используйте скрипты:
   python check_glb_model.py <файл.glb>
   python adjust_glb_model.py <файл.glb>

======================================================================
```

---

## 🔧 Исправление проблем

### ⚠️ Единицы не в миллиметрах

**Проблема:** `Unit System: METRIC, Length Unit: METERS`

**Решение:**
1. `Scene Properties` (иконка сцены справа)
2. `Units → Unit System = Metric`
3. `Length = Millimeters`

---

### ⚠️ Масштаб не равен 1.0

**Проблема:** `Scale: X: 2.5, Y: 2.5, Z: 2.5`

**Решение:**
1. Выделите объект
2. `Ctrl+A` → `Scale`
3. Или: `Object → Apply → Scale`

---

### ⚠️ Origin не в нижнем центре

**Проблема:** `Origin в мировых координатах: (50.0, 30.0, 100.0)`

**Решение — Способ 1 (автоматически):**
1. Выделите объект
2. `Object → Set Origin → Origin to Geometry`

**Решение — Способ 2 (точно в нижний центр):**
1. `Tab` → Edit Mode
2. Выделите все вершины (`A`)
3. `Mesh → Transform → Geometry to Origin`
4. В Object Mode:
5. Установите 3D Cursor в (0, 0, 0): `Shift+S → Cursor to World Origin`
6. `Object → Set Origin → Origin to 3D Cursor`
7. Переместите модель, чтобы низ был на Z=0

---

### ⚠️ Есть неприменённые модификаторы

**Проблема:** `Модификаторов: 2 • Solidify, Mirror`

**Решение:**
1. Выделите объект
2. `Ctrl+A` → `All Modifiers`
3. Или: `Object → Apply → All Modifiers`

---

## 🎯 Идеальная модель для экспорта

После запуска скрипта все пункты должны быть ✅:

```
✅ Единицы: МИЛЛИМЕТРЫ
✅ Объект в начале координат (0, 0, 0)
✅ Вращение = 0
✅ Масштаб = 1.0
✅ Origin в НИЖНЕМ ЦЕНТРЕ
✅ Модификаторов нет (или применены)
```

Только тогда модель будет корректно экспортироваться в GLB!

---

## 🔄 Рабочий процесс

1. **В Blender:**
   - Создайте модель
   - Запустите `blender_analyze_model.py`
   - Исправьте все ⚠️ предупреждения
   - Экспортируйте GLB

2. **В командной строке:**
   ```powershell
   python check_glb_model.py test.glb
   python adjust_glb_model.py test.glb test_fixed.glb
   ```

3. **В проекте:**
   - Загрузите `test_fixed.glb` в `public/assets/models/`
   - Обновите путь в `configurator.js`
   - Установите `scale.set(1, 1, 1)` — масштаб не нужен!

---

## 💡 Полезные горячие клавиши Blender

- `Alt+P` — запустить скрипт
- `Ctrl+A` → Scale — применить масштаб
- `Alt+G` — сбросить позицию
- `Alt+R` — сбросить вращение
- `Shift+S` → Cursor to World Origin — курсор в (0,0,0)
- `Tab` — переключение Edit/Object Mode
- `A` — выделить всё

---

Теперь у вас есть полный контроль над моделью прямо в Blender! 🚀
